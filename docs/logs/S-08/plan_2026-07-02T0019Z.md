# Execution Plan — S-08(2026-07-02)

**Scope contract:** `docs/logs/S-08/scope_2026-07-02T0019Z.md`
**Approved:** 2026-07-02(user approve at Phase 1 gate)
**Dispatch units:** 6(其中 AS-37-u2 條件觸發)
**Critical path:** ~14h subagent work(AS-37 選 (b) 則 ~18h)— **全序列**,因共用 UE host(build mutex + `Saved/Logs/ArchSim.log` 單一日誌,平行會互踩)
**Session cap check:** 6 units ≤ 10 ✅

## Dispatch graph

```
AS-36-u1 ──────→ NITS-u1 ─────────────┐
    │                                  ├──→ AS-08-u1 ──→ AS-08-u2
    └──(portal frame 可解)────────────┘         ↑
AS-37-u1 ──→ [user 選 (a)/(b)] ──→ AS-37-u2(僅(b))│
    └──(commandlet spawn 邊界知識)──────────────┘
```

Dispatch 順序:AS-36-u1 → AS-37-u1 → NITS-u1 → [AS-37-u2 if (b)] → AS-08-u1 → AS-08-u2

## Unit 明細

### 1. AS-36-u1 — PlaceKSetMember 雙柱同 node-pair root-cause + fix + test

```yaml
unit_id: AS-36-u1
parent_task: AS-36
budget_hours: 3
budget_tokens: 250000
budget_steps: 100        # UE build-iterative,HANDOFF v0.5.1 教訓 #6 校準
budget_timeout: 30min
domain_skills: [ue5-engineer (primary), cpp-engineer (secondary)]
production_code_delta_estimate: 40
test_delta_estimate: 60
files_likely_touched:
  - Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp (fix)
  - Source/ArchSim/Private/Tests/ArchSimScenarioFixtureTest.cpp (新 sub-check)
  - Source/ArchSim/Private/Tests/ArchSimPortalFramePIESmokeTest.cpp (視情況強化 assert)
depends_on: []
unlocks: [NITS-u1, AS-08-u2]
adversarial_focus:
  - root cause 須有數值 trace(兩柱端點座標逐步算出,file:line 佐證)
  - fix 後 commandlet PIE SC2b 須顯示相異 node pair + HeatmapActor 真的 spawn(leg 6 log 證據)
  - 既有 ScenarioFixture SC1-SC7 與 leg 6 不退化
  - 新 assert 是「node pair 相異」不是只「solve 成功」
  - run_gate ExpectedUeTests 若動,SKIP≠PASS 紀律
escalate_triggers:
  - root cause 指向 FrameCore engine(FROZEN)
  - 修法需動 ArchSim.uproject
```

### 2. AS-37-u1 — ALS commandlet crash 查證(read-only)

```yaml
unit_id: AS-37-u1
parent_task: AS-37
budget_hours: 2
budget_tokens: 200000
budget_steps: 60
budget_timeout: 25min
domain_skills: [ue5-engineer (primary)]
production_code_delta_estimate: 0   # read-only 查證
test_delta_estimate: 0
files_likely_touched: []            # scratch 全清,不留檔
depends_on: []
unlocks: [AS-37-u2 (conditional), AS-08-u2 (sidestep 知識)]
adversarial_focus:
  - crash 重現證據 verbatim(callstack / log 行)
  - 定位到具體哪個 LoadObject 何時失敗(ALS + UE source file:line)
  - commandlet vs editor plugin content mount 時序差異的證據鏈
  - (a)/(b) 成本風險誠實並列,不預選立場
escalate_triggers:
  - 查證發現 production 路徑(user-driven PIE)也受影響 → 升級severity回報
output_gate: 報告完成 → AskUserQuestion 讓 user 選 (a) 文件化 / (b) 深挖 fix
```

### 3. NITS-u1 — v0.5.1 三個 cosmetic NITs

```yaml
unit_id: NITS-u1
parent_task: deferred-NITs(v0.5.1 HANDOFF §4)
budget_hours: 1
budget_tokens: 150000
budget_steps: 40
budget_timeout: 20min
domain_skills: [cpp-engineer (primary)]
production_code_delta_estimate: 0   # test + script only
test_delta_estimate: 30
files_likely_touched:
  - Source/ArchSim/Private/Tests/ArchSimPortalFramePIESmokeTest.cpp (DEFINE 順序 reorder)
  - Scripts/run_pie_gate.ps1 (stderr 可見度 + stale-log 時戳防護)
depends_on: [AS-36-u1]   # 同檔碰撞 force-sequence(PIE smoke test 檔)
unlocks: [AS-08-u2]
adversarial_focus:
  - 0 行為變化(DEFINE reorder 不改 RunTest 執行語意;latent command 註冊順序不變)
  - stale-log 時戳防護不得引入 false-FAIL(parser 韌性)
  - regex 維持 ASCII-only(PS5.1 CJK ArgumentException 教訓)
  - leg 6 改動後仍 PASS
escalate_triggers:
  - 任一 NIT 發現其實是行為 bug(非 cosmetic)→ 停,回報
```

### 4. AS-37-u2 — ALS LoadObject timing fix(條件:user 選 (b) 才 dispatch)

```yaml
unit_id: AS-37-u2
parent_task: AS-37
conditional: true         # 僅 user 在 AS-37-u1 output gate 選 (b) 時 dispatch
budget_hours: 4
budget_tokens: 250000
budget_steps: 100
budget_timeout: 30min
domain_skills: [ue5-engineer (primary), cpp-engineer (secondary)]
production_code_delta_estimate: 60
test_delta_estimate: 20
files_likely_touched:
  - Source/ArchSim/Private/Characters/ArchSimCharacter.cpp
  - Plugins/ALS/...(僅在揭露 + patch 檔留底下)
  - Tools/patches/(新 patch 檔)
depends_on: [AS-37-u1, user-decision-(b)]
unlocks: []
adversarial_focus:
  - patch 紀律(S-06 U-ALS 前例:Tools/patches/ 檔 + git apply 可重放)
  - user-driven PIE 路徑不可退化(本 session 無人驗 → 自動化證據 + 誠實 limitation 註記)
  - ALS plugin delta 最小化 + 逐行揭露
escalate_triggers:
  - fix 需動 ALS 超過 ~30 LOC → 停,回報
```

### 5. AS-08-u1 — SPUD save/load 生產接線 + RF_Transient audit

```yaml
unit_id: AS-08-u1
parent_task: AS-08
budget_hours: 4
budget_tokens: 300000
budget_steps: 100
budget_timeout: 30min
domain_skills: [ue5-engineer (primary), cpp-engineer (secondary)]
production_code_delta_estimate: 150
test_delta_estimate: 60
files_likely_touched:
  - Source/ArchSim/Public/Subsystems/(新 orchestration 類或 GameInstance 掛點)
  - Source/ArchSim/Private/Subsystems/
  - Source/ArchSim/Public/Components/ArchSimMemberData.h(視需要 ISpudObject 標記)
  - Config/DefaultEngine.ini(僅必要 SPUD 設定)
depends_on: [AS-36-u1, AS-37-u1]   # scope contract 指定順序;非硬檔案依賴
unlocks: [AS-08-u2]
adversarial_focus:
  - RF_Transient audit 逐 spawn-path file:line(SPUD reflection 掃描條件從 SPUD source 讀出)
  - load 後 registry 重建語意:re-register → 150ms debounce → re-solve 鏈完整
  - 不新增對 deprecated StructUtils API 的直接依賴(SPUD 內部依賴照舊)
  - ArchSim.uproject 0 行(鐵則 #5)
  - 誠實分級:[VERIFIED headless] vs [NEW CODE, PIE required]
escalate_triggers:
  - 需改 Plugins/SPUD/ 源碼(超出 .uplugin metadata)→ 先揭露再動
  - save/load 語意需要動 FrameCoreUE(需評估是否波及 FROZEN 邊界)
```

### 6. AS-08-u2 — Save/load PIE smoke test + gate 接線

```yaml
unit_id: AS-08-u2
parent_task: AS-08
budget_hours: 4
budget_tokens: 300000
budget_steps: 100
budget_timeout: 30min
domain_skills: [ue5-engineer (primary)]
production_code_delta_estimate: 0
test_delta_estimate: 300
files_likely_touched:
  - Source/ArchSim/Private/Tests/ArchSimSaveLoadPIESmokeTest.cpp(新;AS-35 LatentCommand 模板)
  - Scripts/run_gate.ps1(leg 2 PIE 排除維持 + ExpectedUeTests 雙主機數字)
  - Scripts/run_pie_gate.ps1(多 PIE 測試 wiring)
depends_on: [AS-08-u1, AS-36-u1, AS-37-u1, NITS-u1]
unlocks: []
adversarial_focus:
  - PIE test 用 AS-35 模板 + DefaultGameMode sidestep(AS-37-u1 知識)
  - save 檔 artifact 清理(Saved/ 不落 commit;test teardown 刪 slot)
  - 斷言「還原後 member/node/util 狀態」而非只「不 crash」
  - run_gate leg 2 filter:PIE category 仍排除(HANDOFF §3 警告);SKIP≠PASS
escalate_triggers:
  - commandlet PIE 下 SPUD subsystem 不可驅動(S-01 stub 教訓重演)→ 停,回報測試策略選項
```

## Ordering rationale(Experimental)

風險最高的兩個未知(AS-36 root cause、AS-37 crash 定位)放最前 fail-fast;AS-08 最大件放最後,吃前面所有結論(portal frame 可解 + commandlet spawn 邊界 + script 檔已穩)。全序列是 host 資源約束(UE build mutex / `Saved/Logs/ArchSim.log` 單一日誌),非依賴使然。平行碰撞檢測:AS-36-u1 × NITS-u1 同檔(`ArchSimPortalFramePIESmokeTest.cpp`)、NITS-u1 × AS-08-u2 同檔(`run_pie_gate.ps1`)→ 皆已 force-sequence。

## Session-wide mechanical stops

| Stop | 值 |
|---|---|
| Subagent output tokens | 300K cap / dispatch |
| Wall-clock / dispatch | 30 min hard timeout |
| Tool-call step cap | **UE build-iterative units 100(⚠️ 高於 hub 預設 50,依 HANDOFF v0.5.1 教訓 #6 校準;user 已在 plan 審批中看到)**;investigation 60;cosmetic 40 |
| Adversarial re-prompt | 3 BLOCKER cycles → AskUserQuestion |
| Spokes / round | 全序列(host 約束),無平行 |
| Session task count | 6 / 10 cap ✅ |

## Tag 節點預期(Phase 4/6 最終裁定)

- AS-36-u1 → v0.5.2 patch
- AS-37-u1 → log-only,無 tag
- NITS-u1 / AS-37-u2 → 隨後續 tag(或獨立 patch,Phase 4 裁定)
- AS-08-u1 + u2 收完 → Phase 6 Mode A **v0.6.0** minor bump

## Critical path detail(理想化,review/release 開銷另計)

```
T=0     AS-36-u1 dispatch(3h)
T=3h    review + release → AS-37-u1 dispatch(2h)
T=5h    review + user 決策 gate → NITS-u1 dispatch(1h)
T=6h    review + release → [AS-37-u2 4h if (b)]
T=6-10h AS-08-u1 dispatch(4h)
T=10-14h review + release → AS-08-u2 dispatch(4h)
T=14-18h 最終 review + release-hardening → Phase 6 close(v0.6.0)
```
