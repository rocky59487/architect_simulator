# Scope Contract — S-07 Session 2026-06-28T0938Z

**Sprint:** S-07 (open — first scope after S-06 close on v0.5.0)
**Target version:** v0.5.0 → v0.5.1 (patch — Mode B single-patch per release-hardening Phase 6)
**Budget estimate:** 3-5 hours (subagent work)
**Author:** /work phase-0 from user brief + memory pre-flight (`v0-5-0-pie-auto-smoke-architecture.md` + `pie-auto-smoke-preference.md`)

---

## 1. Goal (success criterion)

**v0.5.1** tagged with:

- NEW C++ Automation Test class `ArchSim.PIE.PortalFrameSmoke` using UE Automation
  Test framework + LatentCommands(`FStartPIECommand(false)` / `FWaitForMapToLoadCommand`
  / custom `DEFINE_LATENT_AUTOMATION_COMMAND_*` for portal-frame drive / `FTakeActiveEditorScreenshotCommand` / `FEndPlayMapCommand`)
- New test dispatchable via `UnrealEditor-Cmd.exe ArchSim.uproject -ExecCmds="Automation RunTests ArchSim.PIE.PortalFrameSmoke; Quit" -unattended -log`
- Screenshot artifact `v0_5_x_pie_smoke.*` written under `Saved/Screenshots/`
- Test integrated as **6th leg** of `Scripts/run_gate.ps1` (or new `Scripts/run_pie_gate.ps1` invoked by run_gate)
- `$ExpectedUeTests` bumped 149 → 150 (cuDSS) / 147 → 148 (non-cuDSS)
- 6-leg gate ALL PASS on integrator host
- Mode B single-patch release published (commit + tag `v0.5.1` + RELEASE_v0.5.1.md + HANDOFF_v0.5.1.md)
- Memory `v0-5-0-pie-auto-smoke-architecture.md` rewritten to «AS-35 PASS; canonical PIE gate live»

---

## 2. Tasks (ordered, with budget)

| Order | ID | Title | Budget | Domain skill(s) | Dependency |
|---|---|---|---|---|---|
| 1 | AS-35-u1 | PIE Auto-Smoke C++ test class + Build.cs deps + run_pie_gate.ps1 + run_gate.ps1 6th-leg integration + $ExpectedUeTests bump | 3-5h | ue5-engineer + cpp-engineer | none |

**Note:** Treated as 1 cohesive unit. If Phase 3 review surfaces integration issues
(LatentCommand timing / render-thread / commandlet boot), the BLOCKER → re-dispatch
protocol covers iteration; only escalate to splitting into AS-35-u1 (test class) +
AS-35-u2 (gate wiring) if iteration count > 2 on the same root cause.

---

## 3. Constraints

### FROZEN paths (iron rules #1 + #2 — 0-line touch)
- `Plugins/FrameSolver/Source/FrameCore/` (engine since v4.0.0)
- `Plugins/LevelSim/Source/LevelCore/` (LevelSim since v2.2+1)

### Source delta confined to
- `Source/ArchSim/Private/Tests/ArchSimPortalFramePIESmokeTest.cpp` (NEW)
- `Source/ArchSim/ArchSim.Build.cs` (PrivateDependency add, IF needed — verify against `UnrealEd` / `AutomationController` requirement)
- `Scripts/run_pie_gate.ps1` (NEW, OR inline into run_gate.ps1 as 6th leg block)
- `Scripts/run_gate.ps1` (`$ExpectedUeTests` bump + 6th-leg invocation)
- `docs/RELEASE_v0.5.1.md` (NEW)
- `docs/HANDOFF_v0.5.1.md` (NEW)
- `docs/ARCHITECTURE_INDEX.md` (latest-tag line + § 6 test inventory append + § 7 backlog AS-35 row + § 8 cheat-sheet 6-leg note)
- `CLAUDE.md` 「現況」block (insert v0.5.1 entry, demote v0.5.0)
- `docs/logs/S-07/manager.md` (Phase 5 per-cycle entry + Phase 6 SESSION CLOSE block)
- `docs/logs/S-07/agent_AS-35-u1.md` (Phase 2 dispatch log + Phase 3 review verdict + Phase 4 release section)

### Hard PROTECTED (no touch under any circumstance)
- `ArchSim.uproject`
- `.gitignore`
- `Plugins/LevelSim/` (entire dir)
- Build artifacts (`*.dll`, `*.exp`, `*.lib`, `Binaries/`, `Intermediate/`)
- v0.5.0 commit `069bc42` or its tag (no amend, no re-tag, no force-push)

### Salvage allowed from reflog (per memory note)
- `git show ebe2dad:Scripts/launch_pie_smoke.ps1` — pre-flight scaffolding
- `git show ebe2dad:Scripts/run_pie_auto_smoke.ps1` — result-parsing scaffolding
- (Reflog SHAs `ebe2dad` / `d1e3e7d` from rolled-back v0.5.0 attempt; PowerShell wrapper code may be salvageable; **NOT** the Python `run_pie_auto_smoke.py` — architecturally dead per `v0-5-0-pie-auto-smoke-architecture.md`)

---

## 4. Risk profile

**Experimental** — LatentCommand framework details + render-thread/PIE timing tuning
typically requires 2-3 iterations before stable. Subagent should expect iteration
and write a thorough Phase 2 verification log.

Qualifier: This is **infrastructure**, not gameplay; no end-user regression risk
even if the new test itself is flaky. Worst case = test marked `EditorContext only`
and excluded from gate count if it cannot stabilize within budget.

---

## 5. Audience

**Primary:** 自己 — test infrastructure investment; eliminates the recurring 5-10 min
per-release manual PIE smoke (per `pie-auto-smoke-preference.md` "automate routine,
keep gameplay-feel manual" principle).

**Secondary:** 答辯委員 (graduation defense) — demo-able auto gate counts as +1
maturity / engineering-discipline signal.

**Explicit non-audience:** 試玩學生 / open-source community — no end-user-facing
surface added; this is repo-internal infra.

---

## 6. Anti-goals

- DO NOT amend `v0.5.0` commit `069bc42`, re-tag, or force-push
- DO NOT touch FROZEN paths (FrameCore / LevelCore) under iron rules #1/#2
- DO NOT bring back Python `-ExecutePythonScript` PIE-driving path
  (verified architectural dead end per `v0-5-0-pie-auto-smoke-architecture.md`
  attempts 1 + 2 — Python blocks game thread, slate tick callbacks stop firing
  through PIE transition)
- DO NOT use `time.sleep` / `register_slate_post_tick_callback` for PIE waits
  (failure mode of the dead Python path)
- DO NOT use `editor_play_simulate()` / `FStartPIECommand(true)` —
  Simulate mode has no PlayerController, won't drive ALS pawn; use
  `FStartPIECommand(false)` for actual PIE
- DO NOT use `AutomationLibrary.take_high_res_screenshot` from Python in `-nullrhi`
  mode (render thread dead); use C++ `FTakeActiveEditorScreenshotCommand`
- DO NOT include `-nullrhi` flag when invoking the new PIE test commandlet
  (need render thread alive for screenshot capture)
- DO NOT bump major/minor version — Mode B = patch only (v0.5.0 → v0.5.1)
- DO NOT add ANY new feature beyond PIE auto-smoke scope; no creep into
  AS-04 (Plugins panel) / AS-05 (art assets) / AS-08 (SPUD audit) / AS-29 (gate race diagnosis)
- DO NOT touch `Source/ArchSim/` files outside the new test file unless
  Build.cs gen requires it (and the change must be additive PrivateDependency-only)

---

## 7. ESCALATE conditions (Phase 2 subagent must STOP and emit `## ESCALATE`)

1. **LatentCommand UE 5.7 semantic drift** — `FStartPIECommand` / `FWaitForMapToLoadCommand`
   behavior differs from `Engine/Source/Editor/UnrealEd/Public/Tests/AutomationEditorCommon.h`
   documented signature in a way that breaks the smoke flow
2. **Render-thread gating conflict** — new test requires render thread but commandlet boot
   only stabilizes under `-nullrhi`; cannot reconcile screenshot requirement
3. **HeatmapActor commandlet absence** — `AFrameUtilizationHeatmapActor` does not spawn in
   commandlet-mode PIE even with render thread alive (PIE-world Registry context isolation
   or procedural-mesh init dependency on a Slate viewport)
4. **Module dependency cycle** — `IMPLEMENT_COMPLEX_AUTOMATION_TEST` requires `UnrealEd` /
   `AutomationController` PrivateDependency that triggers a circular dependency with existing
   ArchSim or FrameCoreUE modules
5. **`WITH_EDITOR` gating blocks reach** — `UArchSimScenarioWidget::SpawnDefaultPortalFrame`
   BP-callable cannot be invoked from C++ Automation Test because the widget class is
   `#if WITH_EDITOR` gated and the test TU loses access at commandlet link time
6. **Budget overrun** — estimated remaining work exceeds 1.5x budget (> 7.5h) without
   convergence to PASS
7. **Iron rule pressure** — solution requires touching FROZEN paths (FrameCore / LevelCore);
   STOP and request CLAUDE.md amendment in chat (iron rule 1b: verbal amendment ≠
   amendment, must commit CLAUDE.md change in same release)

---

## 8. Hard gate v0.5.1

- 6-leg gate ALL PASS on integrator host (`Scripts\run_gate.ps1 -RequireOpenSees`)
  - Leg 1: standalone F1..F71
  - Leg 2: UE automation (150 cuDSS / 148 non-cuDSS — bumped from 149/147)
  - Leg 3: OpenSees strict
  - Leg 4: linear_deep_audit 104 checks
  - Leg 5: CLI roundtrip
  - **Leg 6 (NEW):** PIE auto-smoke (`ArchSim.PIE.PortalFrameSmoke`) PASS via UnrealEditor-Cmd commandlet
- Screenshot artifact written to `Saved/Screenshots/v0_5_x_pie_smoke.*` (size > 1KB sanity)
- Result parser correctly extracts `Result=成功` (or English equivalent) from `Saved/Logs/ArchSim.log`
- Memory `v0-5-0-pie-auto-smoke-architecture.md` rewritten to «AS-35 LIVE» state

---

## 9. Phase chain expectation

- Phase 0 (this) → lock contract → write to disk → AskUserQuestion lock gate
- Phase 1 → plan AS-35-u1 single-dispatch-unit decomposition; user approval
- Phase 2 → dispatch AS-35-u1 to ue5-engineer subagent with pre-flight reads
  (UE 5.7 AutomationEditorCommon.h/cpp + ArchSimScenarioFixtureTest.cpp + ArchSimCharacterTest.cpp
  + memory `v0-5-0-pie-auto-smoke-architecture.md` skeleton)
- Phase 3 → adversarial review (verify LatentCommand pattern, render-thread claim,
  ExpectedUeTests math, no FROZEN touch)
- Phase 4 → release-hardening Mode B single-patch (v0.5.0 → v0.5.1)
- Phase 5 → docs sync (ARCH_INDEX § 6 + § 7 AS-35 row + § 8 6-leg note + CLAUDE.md 現況)
- Phase 6 → SESSION CLOSE (state file → idle); single-patch release means no minor bump

---

## 10. Memory references (subagent pre-flight)

- `~/.claude/projects/E--project/memory/v0-5-0-pie-auto-smoke-architecture.md` — AS-35 skeleton + LatentCommand reference + "What NOT to try" 5 lessons + reflog SHAs for salvage
- `~/.claude/projects/E--project/memory/pie-auto-smoke-preference.md` — user hybrid design principle (automate routine, manual gameplay-feel)
- `E:\project\UE_5.7\Engine\Source\Editor\UnrealEd\Public\Tests\AutomationEditorCommon.h` — LatentCommand public API ground truth
- `E:\project\UE_5.7\Engine\Source\Editor\UnrealEd\Private\Tests\AutomationEditorCommon.cpp` — LatentCommand impl ref
- `Source/ArchSim/Private/Tests/ArchSimScenarioFixtureTest.cpp` — AS-30 IMPLEMENT_SIMPLE_AUTOMATION_TEST convention (most similar prior art)
- `Source/ArchSim/Private/Tests/ArchSimCharacterTest.cpp` — AS-03 ALS regression pattern
- `docs/RELEASE_v0.5.0.md` + `docs/HANDOFF_v0.5.0.md` — v0.5.0 baseline + revert disclosure section confirming Python path dead

---

**Lock signature:** awaiting Tier 3 user confirmation via AskUserQuestion.
