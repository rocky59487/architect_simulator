# Scope Contract — Sprint S-04 (2026-06-26T20:30 Asia/Taipei)

> Locked by user via `/work` Phase 0 Tier 3 commit gate.
> Generated by `work-phase-0-questioning` skill.
> Baseline tag: `v0.3.0` (commit `442670c`, S-03 closed 2026-06-26T20:00).

---

## Header

- **Sprint:** S-04
- **Target version:** `v0.3.1` patch → (conditional `v0.4.0` minor)
- **Budget estimate:** open (no cap — user explicit; auto-stop at unit-level via Phase 2 mechanical stops)
- **Risk profile:** Experimental (sandbox spikes permitted in `Research/`; main worktree stays safe)
- **Audience primary:** 試玩學生 (playable bar); secondary: 答辯委員 + GitHub community

---

## 1. Goal (single-sentence success criterion)

Ship `v0.3.1` patch consolidating S-03 leftovers (AS-20 + AS-24 + 6 cosmetic Phase-5 doc NITs + cross-session hook race fix), then run sequential spike evaluations (UE5.8 upgrade in `Research/ue58_attempt/` sandbox → Scenario editor MVP on `v0.3.1` base) to a clean go/no-go decision; bump `v0.4.0` minor ONLY if both spikes evaluate green AND Scenario MVP is playable end-to-end in PIE. If either spike does not evaluate clean, park its outcome under `Research/` without contaminating main.

## 2. Tasks (ordered, with budgets and dependencies)

| # | ID | Title | Budget | Domain skill(s) | Dependency |
|---|---|---|---|---|---|
| 1 | `U-INFRA-u1` | Hook state-file race per-project-id / per-session-id fix (S-03 retrospective lesson #6) | 30 min | infra (cpp-style edit on `~/.claude/hooks/work-phase-guard.ps1`) | none |
| 2 | `AS-20-u1` | LogTemp → LogArchSim category upgrade (umbrella `LogArchSim` vs per-class `LogArchSimMember` to be decided in Phase 1) | 1-1.5h | cpp-engineer + ue5-engineer | none (parallel with #1) |
| 3 | `AS-24-u1` | FrameCoreUE NewObject outer for InteractiveSubsystem isolated runs (`GetTransientPackage()` pattern) | 30-45 min | cpp-engineer + ue5-engineer | none (parallel with #1 + #2) |
| 4 | `PHASE5-NITS-u1` | 6 cosmetic doc NITs bundle: (a) `ArchSimPieHarness.h:52-54` docstring "always" overclaim, (b) `ArchSimPieDriverLoopTest.cpp:108-111` sub-check 4 tautology, (c) `ArchSimCharacter.cpp:153` warn prefix `(ArchSim|Input)`, (d) `run_gate.ps1` stale `142/140` comment trim, (e) `ARCHITECTURE_INDEX.md §5` `< 96` reading ambiguity + `§6` RebaselineCeiling `cpp:281` cite, (f) test rename `FrameCore.UE.EmptyModelStartSession` → `FrameCore.UE.InteractiveSubsystem.EmptyModelStartSession` for namespace parity | 1-1.5h | docs (cpp-engineer for source-side edits) | AS-20 + AS-24 (`ArchSimCharacter.cpp` may co-tenant; sequential after) |
| 5 | `RELEASE-v0.3.1` | Phase 4 release-hardening: bundle commit + annotated tag `v0.3.1` (local only, NOT pushed); update `kEngineVer` if any; `$ExpectedUeTests` recount if test renamed | ~1h | release-hardening skill | #1-#4 |
| 6 | `SPIKE-UE5.8-eval` | Create `Research/ue58_attempt/` sandbox; clone `Plugins/FrameSolver/`, `Plugins/LevelSim/`, `Source/ArchSim/` minimal subset; attempt UE5.8 incremental build; run 5-leg gate against sandbox; record FrameCoreUE pass count delta vs v0.3.1; produce binary delta report; go/no-go decision | 3-5h | ue5-engineer + qa-strategist | #5 |
| GATE-A | (User decision) | UE5.8 spike go vs park-research — user reviews delta report, decides whether to proceed with main-branch UE5.8 migration in a later sprint | — | manager | #6 |
| 7 | `SPIKE-Scenario-MVP` | Scenario editor MVP: in-Editor utility widget that lets a designer drop pre-made K1 (single-room) / K2 (two-room) / K4 (four-room) building footprints into the level, hooks each placement up to `UArchSimMemberData` + `UArchSimModelRegistry`, fires `RequestSolve`, and displays the resulting utilization heatmap via `AFrameUtilizationHeatmapActor`. Playable bar: a 試玩學生 can drop a building, see colored members, and reload the scene without crash. | 4-6h | ue5-engineer + game-designer | #5 (parallel with #6 — independent codepath; on `v0.3.1` base regardless of UE5.8 outcome) |
| GATE-B | (User decision) | Scenario MVP playability check + `v0.4.0` minor bump y/n. If playable green, bump v0.4.0; if not, park as spike-report and stay on `v0.3.1` HEAD | — | manager | #7 |
| 8 | `RELEASE-v0.4.0` | Conditional v0.4.0 minor bump; only fires if GATE-B = playable | ~1h | release-hardening skill | GATE-B = playable |

## 3. Constraints

### Hard file-system constraints

- **FROZEN paths (rule #1 + #2):**
  - `Plugins/FrameSolver/Source/FrameCore/` (engine FROZEN since v4.0.0)
  - `Plugins/LevelSim/Source/LevelCore/` (engine FROZEN since v2.2+1)
- **READ-only paths (external plugins):**
  - `Plugins/ALS/Source/`
  - `Plugins/Prefabricator/Source/`
  - `Plugins/SPUD/Source/`
  - `Plugins/SUQS/Source/`
- **NEVER touch (rule #5):**
  - `.gitignore`
  - `ArchSim.uproject`
  - `Plugins/LevelSim/` entire dir (even Public/)
  - Build artifacts: any `*.dll`, `*.exp`, `*.lib`, `*.obj`, `*.pdb`, `*.exe`

### Sandbox isolation rules

- UE5.8 spike work must live under `Research/ue58_attempt/` and remain `untracked` per `.gitignore` (existing `Research/` is already gitignored).
- Hook fix (`~/.claude/hooks/work-phase-guard.ps1`) lives OUTSIDE this repo — must not appear in any `git diff` of ArchSim; edit-and-test in place, no commit.

### Gate must be green before any commit

- Literal command: `Scripts\run_gate.ps1 -RequireOpenSees`
- Pass predicate: `succeeded == expected` (SKIP ≠ PASS, per rule #3a)
- Expected counts at v0.3.1: 145 cuDSS / 143 non-cuDSS (carry forward from v0.3.0 unless PHASE5-NITS-u1 renames a test → recount)

## 4. Risk profile

**Experimental** — user explicit. Specific shape:

- Patch path (#1-#5) treated as **Safe** sub-band: every commit must pass 5-leg gate, no FROZEN touches, surgical edits only.
- Spike path (#6-#8) treated as **Experimental** sub-band: sandbox crashes acceptable; sandbox eval failures must be honestly logged (no fake green); fail-open by parking under `Research/`.

## 5. Audience

- **Primary:** 試玩學生 (playable) — the Scenario MVP's success criterion is "a student can drop a building, see colored members, reload without crash" on `v0.3.1` base. The patch alone (#1-#5) does not change playability.
- **Secondary:** 答辯委員 (demo-able) + GitHub community (clean docs + release notes when bumped).

## 6. Anti-goals (explicit do-NOT list)

1. Do NOT modify any byte under FROZEN paths. If any unit's plan calls for it → STOP, escalate to user for CLAUDE.md amendment per rule #1b (verbal amendment ≠ amendment).
2. Do NOT `git push` tag(s) or run `gh release create` without explicit user authorization (release-hardening default).
3. Do NOT use `git add -A` / `git add .` — explicit per-file staging only (rule #5).
4. Do NOT pull UE5.8 sandbox spike changes into `main` if `Research/ue58_attempt/` shows binary/test regressions — park as spike-report under `Research/UE58_REPORT.md`.
5. Do NOT bump `v0.4.0` minor if Scenario MVP is not playable per GATE-B — park as spike-report.
6. Do NOT remove, rename (except PHASE5-NITS-u1 #f), or break any v0.3.0-shipped test (145 cuDSS / 143 non-cuDSS baseline).
7. Do NOT touch `~/.claude/*` other than `hooks/work-phase-guard.ps1` (no `settings.json`, no env mutation).
8. Do NOT introduce new `NewObject<UFrameInteractiveSubsystem>()` without proper outer (AS-24 lesson) in any new code path.
9. Do NOT call `git tag` outside Phase 4 / Phase 6 (work-phase-guard hook enforces this).
10. Do NOT mark Phase 5 NITs as "done" if any subagent introduces NEW NITs of the same category (e.g. another `LogTemp` reference, another stale line-ref).

## 7. ESCALATE conditions (subagent must hard-stop and pass to user)

- Any unit requires editing FROZEN engine source → STOP, AskUserQuestion CLAUDE.md amendment.
- UE5.8 sandbox shows >5/60 `FrameCore.UE.*` tests fail OR any standalone `FrameCore.*` test fail → STOP at GATE-A, present binary + test delta.
- Scenario MVP requires a new `FRAMECORE_API` entry → STOP, propose new AS-XX spec to user before touching FrameCoreUE.
- Hook config fix affects other projects' `work-phase-guard.ps1` state file resolution → STOP, present fix surface to user.
- Any subagent attempts `git push` / `gh release create` / `git tag -d` / `git reset --hard` → STOP (release-hardening + work-phase-guard already enforce; ESCALATE logs the attempt).
- Any unit estimate exceeds 1.5x its budget → NOT a STOP (user picked no cap), but emit `log()` notification with revised estimate.

## 8. Definition of Done (per phase)

- **Patch path done:** `v0.3.1` annotated tag at HEAD of bundled commit; 5-leg gate 145/143 PASS; no FROZEN diffs; all 6 NITs visibly fixed in `git diff`; manager.md has CLEAN/NITS verdicts for #1-#5.
- **UE5.8 spike done:** `Research/UE58_REPORT.md` documents (a) sandbox setup steps, (b) build success/failure, (c) test pass delta, (d) plugin compatibility matrix (ALS/SPUD/Prefabricator/SUQS), (e) go/no-go recommendation with rationale.
- **Scenario MVP done:** in-Editor utility widget exists; spawning K1/K2/K4 footprint completes without crash; UtilizationHeatmap renders colored members; PIE reload-scene works once; manager.md has CLEAN/NITS verdict.
- **v0.4.0 release done:** annotated tag; CLAUDE.md "現況" block updated to v0.4.0; HANDOFF_v0.4.0.md written; not pushed.

## 9. Carry-over from prior sprints (not in S-04 scope, listed for transparency)

- AS-04 Plugins panel visual confirmation (~30 min human; user-only)
- AS-05 K1-T2 / K4 art assets (parallel art owner)
- AS-06 SPUD UE5.5 StructUtils deprecation (couples to Z-01; UE5.8 spike outcome decides)
- AS-08 SPUD `RF_Transient` audit (when SPUD orchestration wires up)
- AS-09 non-cuDSS host re-verify (opportunistic; needs separate host)

---

**Approved by user (Tier 3 lock):** 2026-06-26T20:30 Asia/Taipei.
**Next action:** Phase 1 (`work-phase-1-planning`) reads this file and produces ordered dispatch units.
