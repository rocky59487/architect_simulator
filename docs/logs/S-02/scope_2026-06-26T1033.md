# Scope Contract — Session 2026-06-26T1033

**Sprint:** S-02
**Target version:** v0.1.3 → v0.2.0 minor
**Budget estimate:** ~24-30 hours of subagent work
**Locked at:** 2026-06-26T10:33 (local)
**Phase 0 driver:** main thread (Opus 4.7)

---

## Goal (success criterion)

本 session 推進 game-body 到 demo-ready 中段:**AS-10 + AS-02 + AS-03 全 close,
出 v0.2.0 minor tag(user-visible feature ALS pawn 落地)**。AS-05 美術 + AS-04
視覺確認列為 stretch goal,若 session-task-cap 允許做骨架,否則 defer 下個 session。

成功的具體 marker:

- v0.2.0 tag 標在 HEAD
- UE Editor 開 `ArchSim.uproject` 能用角色(ALS pawn)在場景內移動
- 5-leg gate **PASS**(UE count 預計 137 + N 個新 ArchSim.* test)
- HANDOFF_v0.2.0.md 寫好,§4 列出仍 deferred 的 AS-05/AS-04/AS-06/AS-08/AS-09
- `docs/SPRINT_NOTES.md` 補 S-02 完整章節

---

## Tasks (ordered, with budget + dispatch split)

| Order | ID | Title | Budget | Domain skill(s) | Dispatch split | Tag result |
|---|---|---|---|---|---|---|
| 1 | AS-10 | 真實 `PendingRankAccumulation` ceiling test | 3-4h | `cpp-engineer` (test) + `release-hardening` | 1 sub-unit(承 AS-07 context) | v0.1.4 patch |
| 2 | AS-02 | A1-06 GameInstance + Tick driver + BP wire | 8h | `ue5-engineer` + `cpp-engineer` | 3 sub-units (skeleton / driver loop / BP wire + smoke) | v0.1.5 patch |
| 3 | AS-03 | A2-01 ALS pawn 接入 | 12h | `ue5-engineer` (ALS) + `game-designer` (input mapping) | 4 sub-units (Character header / Enhanced Input / Camera / BP+test) | **v0.2.0 minor bump** |
| Stretch | AS-05 + AS-04 | Font import + Plugins panel 視覺確認 | ~4h subagent + 0.5h 人工 | `ue5-engineer` (font import) | 1-2 sub-units(若 session-task-cap 還有空間) | 推到下個 session 收 |

**Dispatch units 合計:1 + 3 + 4 = 8(+ stretch 1-2 → 至多 10,踩 session-task-cap)**

### AS-10 拆解(1 sub-unit)

- 在 `Source/ArchSim/Private/Tests/` 新建 `ArchSimRebaselineTest.cpp`(或 append SaveLoadTest)
- 用 `EAutomationTestFlags::EditorContext` 讓 GameInstance 可用
- 註冊 1 個 member → 96 次 `DeactivateMember` + `RegisterMember` toggle 循環
- assert 第 96 次後 `RequestSolve` 內 force-rebaseline branch 觸發(`ArchSimModelRegistry.cpp:284`)
- 若 Registry 沒 expose `bRebaselineDue` getter → ESCALATE(需動 production header)

### AS-02 拆解(3 sub-units)

- **AS-02a — GameInstance skeleton + Tick hook**(~3h):
  - 新建 `Source/ArchSim/Public/ArchSimGameInstance.h` + `.cpp`
  - 繼承 `UGameInstance`;`Init()` / `Shutdown()` lifetime
  - `Tick(float DeltaSeconds)` override
  - `Config/DefaultEngine.ini` 設 `GameInstanceClass=/Script/ArchSim.ArchSimGameInstance`(這算 `Config/` 改動,**non-frozen**)
- **AS-02b — Tick→sync→RequestSolve driver loop**(~3h):
  - Tick 內取 `UArchSimModelRegistry`,遍歷 `Members[]`
  - 對每個 `UArchSimMemberData` 讀其 `GetOwner()->GetActorLocation()`,比對 registry 內 cached node 位置
  - 若 dirty → 更新 registry(透過尚未存在的 `MoveMemberEndpoint` API 或 deactivate+register 重建)
  - 然後 `RequestSolve()`;debounce 150ms 已內建
- **AS-02c — BP wire + smoke test**(~2h):
  - 加 `ArchSimGameInstanceTest.cpp` 端到端 smoke:spawn 5 actor → move 1 → tick → assert `CachedUtilization` 有變化
  - Namespace `ArchSim.Integration.TickDriver`

### AS-03 拆解(4 sub-units)

- **AS-03a — ArchSimCharacter.h subclass AAlsCharacter**(~3h):
  - 新建 `Source/ArchSim/Public/Characters/ArchSimCharacter.h` + `.cpp`
  - 繼承 `AAlsCharacter`(from ALS-Refactored 4.17)
- **AS-03b — Enhanced Input mapping + InputMappingContext**(~3h):
  - 新建 `Content/Input/IMC_ArchSimDefault`(or via Editor;若 agent 無 UE Editor 路徑則寫 .uasset binary 或 commandlet 腳本)
  - WASD + Mouse Look + Jump + Sprint(ALS 內建 action)
- **AS-03c — Camera mode preset + spawn**(~3h):
  - Camera 預設 third-person(ALS 提供)
  - `DefaultPawnClass` 在 `ArchSimGameMode` 設成 `AArchSimCharacter`
- **AS-03d — BP wire + automation test**(~3h):
  - `ArchSimCharacterTest.cpp`: spawn character → simulate WASD input → assert location 移動
  - Namespace `ArchSim.Gameplay.CharacterInput`

---

## Constraints

- 主線 safe:每個 sub-unit 完工後 **Phase 3 review CLEAN/NITS 才前進**(BLOCKER → 回 Phase 2 重做,cap 3 輪)
- spike 區允許炸:本 session 預計**不進 spike**;若 ALS plugin 撞 build/runtime issue 才會臨時開 spike worktree(用 `EnterWorktree` 或 git worktree)
- session-task-cap **10** 個 dispatch unit(/work hub 預設)
- 5-leg gate 每個 patch 出 tag 前必跑:`Scripts\run_gate.ps1 -RequireOpenSees`
- UE editor build 預期觸發(改 `Source/ArchSim/` → 跑 `Build.bat ArchSimEditor`)
- audience demo-able:v0.2.0 出時 UE Editor 開 ArchSim.uproject 能用角色(ALS pawn)移動
- ALS-Refactored v4.17(已 clone,Plugins/ALS/)的 4.17 tag(HEAD `ba232486`)是 baseline,不準臨時 bump

---

## Risk profile

**主線 safe,spike 區允許炸**

本 session AS-10/AS-02/AS-03 全屬主線 integration,預計不進入 spike。
ALS plugin runtime 撞牆才開 spike worktree(可能性:中,因 ALS 在 UE5.7 已 build 通過 — SPRINT_NOTES Spike 1)。

---

## Audience

- **Primary:** 答辯委員(必須 demo-able)
- **Secondary:** 試玩學生(playable foundation by v0.2.0)+ 開源 community(GitHub release notes + README v0.2 status block)

---

## Anti-goals(explicit do-NOT)

- ❌ 不動 `Plugins/FrameSolver/Source/FrameCore/`(FROZEN since v4.0.0,需 CLAUDE.md amendment)
- ❌ 不動 `Plugins/LevelSim/Source/LevelCore/`(FROZEN since v2.2+1)
- ❌ 不動 `.gitignore`、`ArchSim.uproject`、build artifacts(`.dll/.exp/.lib/.obj/.pdb/.exe`)
- ❌ 不動 v0.1.x 已 shipped 的 test(vacuous A1-07 assertion 保留,HANDOFF_v0.1.3.md §1 規則 — bisect 路徑保留)
- ❌ 不在本 session 推進 **AS-06**(SPUD/StructUtils,等 UE5.8 升前)
- ❌ 不在本 session 推進 **AS-09**(non-cuDSS 重驗,需另一台 host)
- ❌ 不在本 session 寫 production code「配合 test」(AS-07 教訓:test 是 production 行為的契約,不是 spec 的願望投影)
- ❌ 不在 v0.1.4 / v0.1.5 patch 寫 user-visible feature;那是 v0.2.0 的領域
- ❌ 不一次 `git add -A`;每個 sub-unit 結束顯式 add 涉及檔案

---

## ESCALATE 條件(subagent 必須停下問)

| 條件 | 動作 |
|---|---|
| 任何 sub-unit 需動 FROZEN 路徑 | 停下,上變(用 ESCALATE block 回 manager) |
| 單一 sub-unit 實際預算 > 1.5× 估計 | 停下,上變 |
| Phase 3 review BLOCKER 連續 3 輪 | 自動觸發 work-hub escalation-handler |
| session-task-cap 10 觸 | 強制 Phase 6 close,剩餘 task 進下 session |
| UE 137 / 135 gate 跑出未預期 count | 先停下查根因,不硬推進 |
| ALS plugin C4996 deprecation 升等成 error | 停下;考慮 ALS-Refactored 4.18 升級 vs roll-back |
| `Config/DefaultEngine.ini` 改動觸發 makefile invalidation 之外的副作用 | 停下,看 SPRINT_NOTES Spike 1 已知 invalidation 是預期 |
| UE Editor 不啟動 / 不認新 GameInstance class | 停下,查 reflection 註冊 |

---

## Dependency map(用於 Phase 1 ordering verification)

```
AS-10 (test ceiling) -----┐
                          │ shares: ArchSimModelRegistry
AS-02a (GameInstance) ----┤   public API; if AS-10 needs production
AS-02b (Tick driver) -----┤   header changes for bRebaselineDue
AS-02c (BP wire + smoke) -┤   getter, blocks until AS-02a lands
                          │
AS-03a (ArchSimCharacter)─┤  independent of AS-02 mechanically,
AS-03b (Enhanced Input) --┤  but UX-wise paired (character moves
AS-03c (Camera)          -┤  things → AS-02 syncs registry)
AS-03d (BP+test)         -┘
```

**Phase 1 ordering implication:** Run AS-02a first to land GameInstance, then
AS-10 in parallel with AS-02b/c. AS-03 stack runs sequentially after AS-02 lands.

---

## Phase 1 input

This contract is the input to `work-phase-1-planning`. Phase 1 will:
- Verify the dispatch split is granular enough
- Risk-order the dispatch list
- Estimate per-dispatch token budget
- Assign domain skills per dispatch unit
- Request user approval before Phase 2 starts

---

*Sealed at 2026-06-26T10:33 by /work Phase 0 driver. Reviewer-approved.*
