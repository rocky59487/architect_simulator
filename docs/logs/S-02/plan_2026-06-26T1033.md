# Execution Plan — Session 2026-06-26T1033

**Scope contract:** `docs/logs/S-02/scope_2026-06-26T1033.md`
**Total dispatch units:** 8(+ stretch 1-2)
**Critical path:** ~18h subagent + ~4h Phase 4 release ceremony(2 parallel max,3 tags)
**Sequential estimate:** ~24h subagent if no parallelism
**Approved at:** 2026-06-26T10:33 (local)
**Phase 1 driver:** main thread (Opus 4.7)

---

## Dispatch graph (DAG, no cycles)

```
AS-02a (GameInstance + FTickableGameObject + Config)
         │
         ├──────────────────┐
         ▼                  ▼
      AS-10            AS-02b (Tick driver loop)
   (ceiling test)            │
   ─── v0.1.4 tag ───         ▼
                          AS-02c (Smoke + BP)
                          ─── v0.1.5 tag ───
                                │
                                ▼
                          AS-03a (ArchSimCharacter)
                                │
                          ┌─────┴─────┐
                          ▼           ▼
                      AS-03b      AS-03c
                  (Enhanced Input) (Camera + GameMode)
                          └─────┬─────┘
                                ▼
                          AS-03d (Test + BP)
                          ─── v0.2.0 minor tag ───
```

**Dispatch order (sequential, safe profile):** AS-02a → AS-10 → AS-02b → AS-02c → AS-03a → AS-03b → AS-03c → AS-03d

---

## Unit details

### AS-02a — GameInstance skeleton + FTickableGameObject + Config wire

- **Budget:** 3h / 200K tokens / 40 steps / 25min
- **Domain:** `ue5-engineer (primary)` + `cpp-engineer (secondary)`
- **Files likely touched:**
  - `Source/ArchSim/Public/ArchSimGameInstance.h` (new)
  - `Source/ArchSim/Private/ArchSimGameInstance.cpp` (new)
  - `Source/ArchSim.Build.cs` (already exists,加 `FrameCoreUE` 確認)
  - `Config/DefaultEngine.ini` (set `GameInstanceClass=/Script/ArchSim.ArchSimGameInstance`)
- **Production code delta estimate:** ~120 LOC
- **Test delta:** 0
- **Depends on:** —
- **Unlocks:** AS-10, AS-02b
- **Adversarial focus:**
  - `FTickableGameObject::IsTickable()` 必返回 `bIsActive && GetWorld()` 過濾 CDO + PIE-tear-down
  - `Tick()` 不可 per-frame allocate
  - `GetStatId()` 返回 `STATGROUP_Tickables` 標準 stat
  - `IsTickableInEditor()` 預設 `false`(我們是 game 模式)
  - `IsTickableWhenPaused()` 預設 `false`
- **ESCALATE if:**
  - `FTickableGameObject` 整合需要動 FrameCore engine source
  - GameInstance Tick 跟 RequestSolve debounce 衝突需要動 ArchSimModelRegistry 鎖
- **Tag result:** none(production-only;wait for AS-10 then bundle into v0.1.4)

### AS-10 — Real PendingRankAccumulation ceiling test

- **Budget:** 3-4h / 180K tokens / 35 steps / 25min
- **Domain:** `cpp-engineer (test)` + `ue5-engineer (automation framework)`
- **Files likely touched:**
  - `Source/ArchSim/Public/Subsystems/ArchSimModelRegistry.h` (加 `[[nodiscard]] int32 GetPendingRankAccumulation() const` const getter,純 telemetry;**可能**加 `[[nodiscard]] bool IsRebaselineDue() const`)
  - `Source/ArchSim/Private/Tests/ArchSimRebaselineTest.cpp` (new) 或 append `ArchSimSaveLoadTest.cpp`
  - `Scripts/run_gate.ps1` ($ExpectedUeTests bump 137→138 cuDSS / 135→136 non-cuDSS)
- **Production code delta:** ~10 LOC(只加 const getter,純 telemetry 不改行為)
- **Test delta:** ~140 LOC
- **Depends on:** AS-02a(test fixture 需 GameInstance 存在)
- **Unlocks:** —
- **Adversarial focus:**
  - 第 96 次 toggle 後 ExecuteSolve 內 force-rebaseline 真的觸發(用 `OnSolveComplete` 計數 + getter 證明 `PendingRankAccumulation` reset)
  - 不依賴實作細節(只看公開行為)
  - Test fixture 在 `-nullrhi -unattended` 模式下 GameInstance 可用(`EAutomationTestFlags::EditorContext` 或 `NewObject<UArchSimModelRegistry>` fallback)
  - 加的 getter 為 `[[nodiscard]] const`,不改 behavior
- **ESCALATE if:**
  - 測試需要 invasive friend declaration
  - 需要動 `ArchSimModelRegistry.cpp` production logic
  - 第 96 次 ceiling 行為 vs spec 不符且 spec 不能 honest-correct
- **Tag result:** v0.1.4 patch(after AS-02a accepted)

### AS-02b — Tick driver loop(member sync → RequestSolve)

- **Budget:** 3h / 220K tokens / 45 steps / 25min
- **Domain:** `ue5-engineer (primary)` + `cpp-engineer (secondary)`
- **Files likely touched:**
  - `Source/ArchSim/Private/ArchSimGameInstance.cpp` (擴 Tick 邏輯)
  - `Source/ArchSim/Public/ArchSimGameInstance.h` (擴 dirty-detection 內部 state)
- **Production code delta:** ~80 LOC
- **Test delta:** 0
- **Depends on:** AS-02a
- **Unlocks:** AS-02c
- **Adversarial focus:**
  - Tick 內 dirty detection 必須 O(N) 不 O(N²)
  - Actor 移動偵測用 `GetActorLocation()` 比 cached 而非 transform tick callback
  - 50ms tolerance 避免抖動
  - Tick 跟 ArchSimModelRegistry::DebounceTimer 不相打架(後者已 debounce 150ms)
  - RequestSolve 內 patch 不可累積到 MaxRankBeforeRebaseline=96 以上(會踩 AS-10 同 ceiling)
- **ESCALATE if:**
  - Tick frequency vs DebounceMs 衝突需動 ArchSimModelRegistry
  - 需要 `MoveMemberEndpoint` API 但 Registry 無對應 surface
- **Tag result:** none

### AS-02c — Smoke test + BP accessors

- **Budget:** 2h / 150K tokens / 30 steps / 20min
- **Domain:** `cpp-engineer (test)` + `ue5-engineer (automation framework)`
- **Files likely touched:**
  - `Source/ArchSim/Private/Tests/ArchSimGameInstanceTest.cpp` (new)
  - `Source/ArchSim/Public/ArchSimGameInstance.h` (擴 `[[nodiscard]] GetTickCount()` BP-pure getter)
  - `Scripts/run_gate.ps1` ($ExpectedUeTests bump 138→139 cuDSS / 136→137 non-cuDSS)
- **Production code delta:** ~10 LOC
- **Test delta:** ~150 LOC
- **Depends on:** AS-02b
- **Unlocks:** AS-03a
- **Adversarial focus:**
  - End-to-end: spawn 5 actors → register → tick → move 1 → tick → assert `CachedUtilization` 真有變化
  - `ArchSim.Integration.TickDriver` namespace
  - Test 不假設 frame rate(用 manual `Tick(0.016f)` 推進)
- **ESCALATE if:**
  - End-to-end smoke 失敗根因在 FrameCore solver
  - Tick 推進在 headless `-nullrhi` 模式不執行
- **Tag result:** v0.1.5 patch(after AS-02 stack accepted)

### AS-03a — ArchSimCharacter subclass AAlsCharacter

- **Budget:** 3h / 200K tokens / 40 steps / 25min
- **Domain:** `ue5-engineer (primary, ALS plugin)` + `cpp-engineer (secondary)`
- **Files likely touched:**
  - `Source/ArchSim/Public/Characters/ArchSimCharacter.h` (new)
  - `Source/ArchSim/Private/Characters/ArchSimCharacter.cpp` (new)
  - `Source/ArchSim.Build.cs` (加 `"ALS"` 依賴)
- **Production code delta:** ~80 LOC
- **Test delta:** 0
- **Depends on:** AS-02c
- **Unlocks:** AS-03b, AS-03c
- **Adversarial focus:**
  - Build.cs 加 ALS 依賴後 incremental UE build 不破其他模組
  - `AAlsCharacter` 繼承不踩 ALS-Refactored 4.17 已知限制
  - Character 構造不假設 GameMode 一定存在(SetupPlayerInputComponent 安全)
  - `bUseControllerRotationYaw=false`(ALS 內部 handle)
- **ESCALATE if:**
  - ALS 模組路徑變動需動 `.uplugin` 或 `.uproject`
  - ALS-Refactored 4.17 build 跟 UE5.7 在新 character 場景撞 C4996
- **Tag result:** none

### AS-03b — Enhanced Input mapping + IMC/IA specs

- **Budget:** 3h / 200K tokens / 40 steps / 25min
- **Domain:** `ue5-engineer (Enhanced Input)` + `game-designer (input mapping rationale)`
- **Files likely touched:**
  - `Source/ArchSim/Private/Characters/ArchSimCharacter.cpp` (擴 SetupPlayerInputComponent)
  - `Source/ArchSim/Public/Characters/ArchSimCharacter.h` (加 InputAction `TObjectPtr`)
  - `docs/INPUT_MAPPING.md` (new, IMC + IA UAsset spec 給使用者在 Editor 建立)
- **Production code delta:** ~60 LOC
- **Doc delta:** ~80 行
- **Depends on:** AS-03a
- **Unlocks:** AS-03d
- **Adversarial focus:**
  - WASD + Mouse Look + Jump + Sprint 對應 ALS state machine 正確 trigger
  - IMC 在 BeginPlay 由 `UEnhancedInputLocalPlayerSubsystem::AddMappingContext` 加入
  - IA 用 `BindAction` 透過 `ETriggerEvent::Triggered` 而非 deprecated `IE_Pressed`
- **ESCALATE if:**
  - 必須建立 .uasset binary(無法用文字檔)
  - Editor commandlet 需要超權限
- **Tag result:** none

### AS-03c — Camera mode + ArchSimGameMode wire

- **Budget:** 3h / 200K tokens / 40 steps / 25min
- **Domain:** `ue5-engineer (ALSCamera)` + `cpp-engineer`
- **Files likely touched:**
  - `Source/ArchSim/Public/ArchSimGameMode.h` (new)
  - `Source/ArchSim/Private/ArchSimGameMode.cpp` (new)
  - `Source/ArchSim/Public/Characters/ArchSimCharacter.h` (擴 ALSCamera 依賴)
  - `Source/ArchSim.Build.cs` (加 `"ALSCamera"` 依賴)
  - `Config/DefaultEngine.ini` (set `GameMode` 預設)
- **Production code delta:** ~100 LOC
- **Test delta:** 0
- **Depends on:** AS-03a
- **Unlocks:** AS-03d
- **Adversarial focus:**
  - `ArchSimGameMode` 繼承 `AGameModeBase`(非舊 `AGameMode`)
  - `DefaultPawnClass = AArchSimCharacter::StaticClass()`
  - Third-person camera 用 `UAlsCameraComponent`(ALSCamera 模組)
- **ESCALATE if:**
  - `Config/DefaultEngine.ini` 需要動超出 `[/Script/EngineSettings.GameMapsSettings]` section 範圍
  - GameMode 設定衝突現有 ALS demo 設定
- **Tag result:** none

### AS-03d — Character automation test + BP wire

- **Budget:** 3h / 220K tokens / 45 steps / 25min
- **Domain:** `cpp-engineer (test)` + `ue5-engineer (Enhanced Input simulation)`
- **Files likely touched:**
  - `Source/ArchSim/Private/Tests/ArchSimCharacterTest.cpp` (new)
  - `Scripts/run_gate.ps1` ($ExpectedUeTests bump 139→140 cuDSS / 137→138 non-cuDSS)
- **Production code delta:** 0
- **Test delta:** ~200 LOC
- **Depends on:** AS-03b, AS-03c
- **Unlocks:** —
- **Adversarial focus:**
  - Spawn character → simulate forward input via Enhanced Input → tick 60 frames → assert location.X 真增加
  - `ArchSim.Gameplay.CharacterInput` namespace
  - 不依賴 ALS animation state(用 location-delta 而非 anim variable)
  - run_gate count bump 跟實際 test 數一致(漏 bump 等於假綠)
- **ESCALATE if:**
  - Enhanced Input headless simulation 需要 ALS plugin 內部 friend declaration
  - run_gate.ps1 count bump 撞 cuDSS / non-cuDSS 差距邏輯
- **Tag result:** **v0.2.0 minor bump**(user-visible feature ALS pawn 落地)

---

## Risk-adjusted ordering rationale

User 選「主線 safe + spike 區允許炸」,但所有 8 個 unit 全屬主線 integration(沒 spike)。在 safe profile 下:

1. **AS-02a 必先** — dependency root,GameInstance 不存在前 AS-10/AS-02b 都無法跑
2. **AS-10 + AS-02b 可平行**(共用 AS-02a 但分屬不同檔案 + namespace);**選擇序列跑**(先 AS-10 後 AS-02b)以避免 GameInstance API 在 AS-02b 擴 Tick 邏輯時撞 AS-10 test 假設 → 寧可慢一點換證據可靠
3. **AS-02c smoke 在 AS-02b 後**:smoke 假設 Tick driver loop 完工
4. **AS-03 stack 全在 AS-02 stack 後**:雖技術上 ArchSimCharacter 跟 GameInstance 解耦,但 UX-wise 配對(角色動 → registry sync → solve),寧可確認 AS-02 通了再上 AS-03 避免雙倍 debug
5. **AS-03b ‖ AS-03c 可平行**(不同檔)— 但仍序列跑(safe profile 默認)
6. **stretch goal AS-05 font + AS-04 visual**:若 session-task-cap 還有 2 個 dispatch 空間且時間允許

---

## Critical path detail(序列估計)

```
T=0    AS-02a dispatched          (3h)
T=3    review (AS-02a is production-only; bundles into AS-10 patch)
T=3    AS-10 dispatched           (3-4h)
T=7    review + Phase 4 v0.1.4   (~1h release-hardening + tag)
T=8    AS-02b dispatched          (3h)
T=11   review (AS-02b production-only; bundles into AS-02c patch)
T=11   AS-02c dispatched          (2h)
T=13   review + Phase 4 v0.1.5   (~1h release-hardening + tag)
T=14   AS-03a dispatched          (3h)
T=17   AS-03b dispatched          (3h)
T=20   AS-03c dispatched          (3h)
T=23   AS-03d dispatched          (3h)
T=26   review + Phase 4 v0.2.0 minor (~1.5h release-hardening + tag)
T=27.5 session-close (Phase 6) or stretch goal AS-05/AS-04
```

---

## Session-wide mechanical stops(re-asserted)

| Stop | Threshold | Action |
|---|---|---|
| Subagent output tokens | 300K | 強制 return,escalate |
| Subagent wall-clock | 30min(設 25min per unit 留緩衝) | 強制 return |
| Subagent step cap | 50 tool calls | 強制 return |
| Adversarial re-prompt | 3 BLOCKER 輪 | escalation-handler |
| Session task count | 10 dispatch units | 強制 phase-6-close |
| 5-leg gate fail | 任何 leg fail | 強制 stop,不出 tag |

---

## Phase 2 input

Phase 2 dispatch starts with **AS-02a**(no `depends_on`). On Phase 3 accept,
Phase 2 picks AS-10 next(blocks=[AS-02a] now satisfied).

---

*Sealed at 2026-06-26T10:33 by /work Phase 1 driver. User-approved.*
