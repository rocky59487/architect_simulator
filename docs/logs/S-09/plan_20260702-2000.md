# Execution Plan — S-09(2026-07-02 20:00,user approved)

**Scope contract:** `docs/logs/S-09/scope_20260702-1950.md`
**Total dispatch units:** 5 + Phase 4 release-hardening(單一 v0.6.1 tag)
**Critical path estimate:** ~5.5–6h wall(Round 1 兩單元平行)
**Session-wide budget:** ~19h subagent 工作

## Dispatch graph

```
Round 1(平行,檔案不相交):
  [AS-39-u1 reproducibility]   [AS-40-u1 persistence/registry bug 修+AS-38]
                                        │
Round 2:                        [AS-41-u1 sidecar format v2]
                                        │
Round 3:                        [AS-42-u1 PIE gate 收緊]
                                        │
Round 4:                        [AS-43-u1 claim 誠實化+docs drift]
                                        │
                                [Phase 4 release-hardening → v0.6.1 單一 tag]
```

**Commit 策略:** hooks 擋 phase-4 之外的 commit → 5 單元改動累積於 working tree(每單元過 Phase 3 review + build/測試驗證),Phase 4 做邏輯分組 commit + 單一 v0.6.1 tag + publish(push/gh release 已授權,見 publish-authorization memory)。

---

## AS-39-u1 — Clean-checkout reproducibility

```yaml
unit_id: AS-39-u1
parent_task: AS-39(finding #1)
budget_hours: 3
budget_tokens: 150000
budget_steps: 70
budget_timeout: 40min
domain_skills: [ue5-engineer(scripts/git 向)]
production_code_delta_estimate: 0 LOC C++(純 scripts/docs/patch/git-add)
files_likely_touched:
  - Tools/patches/spud_engineversion_pin.patch (new)
  - Tools/patches/suqs_engineversion_pin.patch (new)
  - Tools/patches/prefabricator_engineversion_pin.patch (new)
  - Tools/patches/README.md (update)
  - docs/THIRD_PARTY.md (new manifest: 4 repo × pinned SHA × patch 清單)
  - Scripts/setup_third_party.ps1 (new; clone→checkout SHA→git apply;冪等)
  - Scripts/run_gate.ps1 (precondition 檢查:plugins 存在 + patch 已 apply)
  - Content/BP_ArchSimScenarioWidget.uasset (git add)
  - Tools/run_pie_smoke_portal.py (處置:track 或刪,附理由)
depends_on: []
unlocks: []
adversarial_focus:
  - patch 在 pinned SHA 上 apply 後 == 現場 diff(bit-exact 驗證)
  - 既有 als_l400_animinstance_guard.patch 與現場 Plugins/ALS diff 一致性
  - setup script 對已存在 checkout 冪等(re-run 不毀損)
  - precondition 缺 plugin / 未 apply patch 時 fail loud;都在時 0 干擾
  - 絕不碰 .gitignore / ArchSim.uproject
  - manifest SHA == 現場各 plugin HEAD(ba232486/a7a6386/284b85d/b7ef0a7)
escalate_triggers:
  - 任一 plugin 現場 HEAD 與預期不符且無法解釋
  - patch 需要改內容本身(scope 說 patch 維持現狀)
```

## AS-40-u1 — Persistence/Registry 真 bug 修 + AS-38

```yaml
unit_id: AS-40-u1
parent_task: AS-40(findings #3 #4 #8 #9 + AS-38)
budget_hours: 5
budget_tokens: 250000
budget_steps: 100
budget_timeout: 60min
domain_skills: [ue5-engineer(primary), cpp-engineer(secondary)]
production_code_delta_estimate: ~120 LOC
test_delta_estimate: ~150 LOC(新 sub-checks)
files_likely_touched:
  - Source/ArchSim/Public/Subsystems/ArchSimPersistenceSubsystem.h
  - Source/ArchSim/Private/Subsystems/ArchSimPersistenceSubsystem.cpp
  - Source/ArchSim/Public/Subsystems/ArchSimModelRegistry.h
  - Source/ArchSim/Private/Subsystems/ArchSimModelRegistry.cpp
  - Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp(AS-38)
  - Source/ArchSim/Private/Tests/ArchSimSpudPersistenceTest.cpp(或對應測試檔)
  - Source/ArchSim/Private/Tests/ArchSimScenarioFixtureTest.cpp(SC8/SC9)
fix_content:
  - "#3: SaveToSlot 空/partial sidecar 覆寫 guard — 空 snapshot + 既有非空 slot → fail loud;缺 component 的 skip-vs-abort 語意定案 + 測試"
  - "#4: Load/Save return contract 誠實化 — missing slot 在 dispatch 前查(讓 header claim 成立);header 註解對齊 async 現實(request-issued vs persisted)"
  - "#9: ReplayLoadedSidecar RegisterMember 失敗 → destroy orphan actor + replay count 不虛增"
  - "#8: Reset() 清 live component bRegistered/MemberIdx;RegisterFixedSupport 改 topology 後 session invalidation(不破 150ms debounce);RegisterMember non-finite endpoint guard"
  - "AS-38: PlaceKSetMember check(Root) → shipping-safe if-guard + log + destroy-and-return;SC8 註解補「node-count==4 才是嚴格保證」;SC9 改走 production path"
depends_on: []
unlocks: [AS-41-u1]
adversarial_focus:
  - 「合法空存檔」路徑不被 guard 誤殺(語意文檔化 + 雙分支測試)
  - Reset 清 flag 不破壞既有 4 個 headless persistence 測試
  - session invalidation 與 debounce timer 的互動(不得重複 EndSession / 不得漏 invalidate)
  - 153 headless 全綠(改動後 rebuild + 至少 ArchSim.* 家族重跑)
evidence_required:
  - 每個 fix 對應 finding 的 file:line 前後對照
  - build 輸出 + 測試運行 log verbatim
escalate_triggers:
  - 修復需動 FrameCoreUE 以外的 plugin 源、或 FROZEN path
```

## AS-41-u1 — Sidecar format v2 完整 persistence

```yaml
unit_id: AS-41-u1
parent_task: AS-41(finding #5)
budget_hours: 6
budget_tokens: 280000
budget_steps: 110
budget_timeout: 60min
domain_skills: [ue5-engineer(primary), cpp-engineer(secondary)]
production_code_delta_estimate: ~250 LOC
test_delta_estimate: ~200 LOC
v2_必涵蓋(或誠實記錄排除理由):
  - material/section library 定義值(非只 index;v1 replay 靠 EnsureDefaultLibraries 只回填 default)
  - member active/deactivated 狀態(DeactivateMember 後的 soft-delete 要 roundtrip)
  - loads:nodalLoads / memberUDLs / shellPressures(FFrameModelDef 可持有;registry 現未寫入仍須 roundtrip 支援)
  - shells
  - 一般 fixity(非 all-fixed 節點)
  - format version 欄 + v1 存檔向前相容(SPUD 缺欄位容忍以 SpudPropertyUtil 行為佐證)
files_likely_touched:
  - Source/ArchSim/Public/Subsystems/ArchSimPersistenceSubsystem.h(v2 records)
  - Source/ArchSim/Private/Subsystems/ArchSimPersistenceSubsystem.cpp(snapshot/replay v2)
  - Source/ArchSim/Public/Subsystems/ArchSimModelRegistry.h(唯讀 accessor 若需)
  - Source/ArchSim/Private/Tests/(新 v2 roundtrip 測試 + v1-compat 測試)
depends_on: [AS-40-u1]
unlocks: [AS-42-u1]
adversarial_focus:
  - 每個 v2 欄位有 roundtrip oracle(populate → save → reset → replay → assert 相等)
  - v1 archive(無 version 欄)載入不 crash、走 default=1 路徑
  - mm/cm 慣例無雙重轉換 drift(v1 支點 mm 慣例保留)
  - GetCurrentModel() snapshot 與 world-walk 兩路徑資料一致
evidence_required:
  - FFrameModelDef 欄位盤點表(涵蓋/排除 + 理由)
  - roundtrip 測試 log verbatim
escalate_triggers:
  - 需動 FrameCore(FROZEN)
  - SPUD 對新增 UPROPERTY 的舊檔行為無法驗證
```

## AS-42-u1 — PIE gate oracle 收緊

```yaml
unit_id: AS-42-u1
parent_task: AS-42(findings #6 #7)
budget_hours: 3
budget_tokens: 200000
budget_steps: 90
budget_timeout: 60min
domain_skills: [ue5-engineer]
production_code_delta_estimate: ~80 LOC(scripts + 測試斷言)
files_likely_touched:
  - Scripts/run_pie_gate.ps1
  - Source/ArchSim/Private/Tests/ArchSimPortalFramePIESmokeTest.cpp
  - Source/ArchSim/Private/Tests/ArchSimSaveLoadPIESmokeTest.cpp
fix_content:
  - "#6: 逐測試名驗證(PortalFrameSmoke + SaveLoadSmoke 都要出現且 PASS,缺一即 FAIL);screenshot freshness(mtime > run start,stale 即 FAIL)"
  - "#7: heatmap 斷言升級(bounded wait 後 hard-assert 或 tracked-actor 硬 oracle;須回應 AS-36 Bug C 當初 soft 化理由);SaveLoad 改 tracked-set oracle(replay 實際 spawn 的 actor 集合取代 world-scan)"
  - 對齊 AS-41 v2 格式斷言
depends_on: [AS-41-u1]
unlocks: [AS-43-u1]
adversarial_focus:
  - 收緊不得引入 flake(leg 6 完整跑至少 1 次 PASS 佐證)
  - stale-pass 場景負向論證(stale screenshot 情境 → 應 FAIL 的推演或實測)
  - PowerShell parser 維持 locale-defensive ASCII-only 慣例(v0.5.1 教訓)
evidence_required:
  - leg 6 run log verbatim(兩測試名 + PASS)
escalate_triggers:
  - heatmap hard-assert 2 次重試內仍 flake → 回報改 tracked-oracle 方案
```

## AS-43-u1 — Claim 誠實化 + docs drift

```yaml
unit_id: AS-43-u1
parent_task: AS-43(findings #2 #10)
budget_hours: 2
budget_tokens: 120000
budget_steps: 50
budget_timeout: 30min
domain_skills: [docs]
files_likely_touched:
  - docs/RELEASE_v0.6.0.md(僅追加 [ERRATA] 區塊,原文零修改)
  - README.md(five-leg→six-leg、test counts、persistence 描述)
  - docs/ARCHITECTURE_INDEX.md(§6 test inventory、§10 latest links、persistence 描述)
depends_on: [AS-42-u1]
unlocks: [Phase 4]
adversarial_focus:
  - git diff 證明 RELEASE_v0.6.0.md 為純追加
  - cross-doc grep 無殘留 stale 字眼(five-leg / 舊 count / v0.2.0 links)
  - persistence 描述與 AS-41 落地現實逐句對齊(不美化)
escalate_triggers:
  - 發現 v2 落地與 claim 仍有落差 → 回報而非美化
```

---

## 風險排序理由

Safe profile → 低風險獨立單元(AS-39)與最高價值 bug 修(AS-40)先行平行(檔案不相交:AS-39 = Tools/Scripts/Content;AS-40 = Source/ArchSim);AS-41 是本 session 最大擴充,放 bug 修之後(乾淨 lifecycle 語意上蓋 v2);AS-42 斷言對齊 v2 後行為;AS-43 最後描述已落地現實。瓶頸 = AS-40→41→42 序列鏈(每單元 UE rebuild + 測試)。

## Session-wide mechanical stops

- Subagent output 300K cap;**per-unit steps/timeout 依上表(70–110 steps、40–60min)— 高於 hub 預設 50/30,依 S-08 retrospective 教訓 #1(214/40 事故)校準,特此標記**
- BLOCKER re-prompt 3 次 → escalate to user
- 平行 spokes ≤ 2(僅 Round 1);session 任務數 5 ≤ 10 ✓
- 子 agent 禁止 spawn agent、禁止停在「等監控」(S-08 教訓 #4,dispatch prompt 明文)

## Critical path

```
T=0      AS-39-u1 ∥ AS-40-u1 dispatch
T≈1h     兩者返回 → Phase 3 review ×2
T≈1.5h   AS-41-u1 dispatch
T≈2.5h   review → AS-42-u1 dispatch
T≈3.5h   review → AS-43-u1 dispatch
T≈4h     review → Phase 4 release-hardening(6-leg gate 全綠 + 嚴格 sweep)
T≈5.5h   v0.6.1 tag + publish → Phase 5 docs → Phase 6 close
```
