# Execution Plan — Session 2026-06-28T0316Z

**Scope contract:** [docs/logs/S-06/scope_2026-06-28T0316Z.md](scope_2026-06-28T0316Z.md)
**Total dispatch units:** 4
**Round structure:** 2 rounds (Round 1 = 3 parallel; Round 2 = 1 sequential)
**Critical path estimate:** 13–17 h(成功路徑);可能延伸 20–25 h 若 U-ALS deep dive 不收斂
**Session-wide subagent budget:** 13–22 h total

---

## Dispatch graph

```
Round 1 (parallel — no file collision verified)
┌──────────────────────────────┐
│ U-IWYU                       │   (Tools/ + docs;independent)
│ U-LOW    ────────────────────┼──→  AS-30 ID rename gate
│ U-ALS                        │   (ArchSimCharacter + Config/ini;independent of widget)
└──────────────────────────────┘
                ↓ (all 3 accept + LOW ID rename in)
Round 2 (sequential — biggest unit)
┌──────────────────────────────┐
│ AS-30 Scenario fixture       │   (ArchSimScenarioWidget extension)
└──────────────────────────────┘
                ↓ (accept)
USER-DRIVEN PIE smoke P1..P15 ← v0.5.0 hard gate
                ↓ (PASS)
Phase 6 close → release-hardening --minor-bump v0.5.0
```

**Parallel collision check (Round 1, manually verified):**
- U-IWYU touches: `Tools/check_iwyu_first_header.py` (NEW) + `Tools/test_iwyu_validator.py` (NEW) + `docs/IWYU_VALIDATOR.md` (NEW) + (optional) `.git/hooks/pre-commit`
- U-LOW touches: `~/.claude/hooks/work-phase-guard.ps1` (OUTSIDE repo) + `docs/ARCHITECTURE_INDEX.md` §7
- U-ALS touches: `Source/ArchSim/Public/Characters/ArchSimCharacter.h` + `Source/ArchSim/Private/Characters/ArchSimCharacter.cpp` + `Config/DefaultEngine.ini` + (maybe) `Plugins/ALS/Source/ALS/Private/AlsCharacter.cpp` (NOT FROZEN per CLAUDE.md §1)

**Overlap on `docs/ARCHITECTURE_INDEX.md`:** U-LOW edits § 7 backlog; AS-30 the unit does NOT edit ARCH_INDEX(Phase 5 docs sync does after AS-30 accept)→ no actual collision. Note: Phase 5 mid-sprint sync runs after each unit accept, sequentially in the main thread → guaranteed serial w.r.t. ARCH_INDEX.

---

## Unit details

### U-IWYU — Pre-commit IWYU first-header validator
- **Parent task:** AS-28-followup(HANDOFF_v0.4.0.1)
- **Budget:** 1.5 h / 80K tokens / 25 steps / 20 min timeout
- **Domain:** cpp-engineer(IWYU 規則理解;Python tool incidental)
- **Production LOC est:** ~80(Python script + optional pre-commit hook)
- **Test LOC est:** ~40(fixture .cpp + integration smoke)
- **Files likely touched:**
  - `Tools/check_iwyu_first_header.py`(NEW)
  - `Tools/test_iwyu_validator.py`(NEW)— pytest 或 unittest 都行
  - `docs/IWYU_VALIDATOR.md`(NEW)— 1-page README
  - `.git/hooks/pre-commit`(optional;若 subagent 認為 OK 否則只給安裝指引 in README)
- **Depends on:** none
- **Unlocks:** none(獨立 tooling)
- **Adversarial focus:**
  - Validator 真的 catch v0.4.0.1 bug pattern(`#include "FrameCoreUE/..."` 出現在 widget cpp 第一個 include,前於 `#include "Editor/ArchSimScenarioWidget.h"`)
  - Pre-commit hook 不擋無關 commit(只 scan `Source/**/*.cpp` 變動,不去管 plugin source)
  - 不破鐵則 #5(script 不做 `git add`,不動 .gitignore,不動 .uproject)
  - 3 個 fixture:positive(正確順序 PASS)+ negative(v0.4.0.1 pattern FAIL)+ edge(只有 comment / 空 include 區段)
  - 跑速度合理(< 1 s on full Source/ tree)
- **ESCALATE if:**
  - Hook 安裝需要動 `.gitignore` 或 `.git/config`(隱私)
  - Validator 想動已存在的 production .cpp 把它「修對」(out of scope — 只報告不改)
  - Python 在 host 沒裝(unlikely;ArchSim 已用 openseespy)

### U-LOW — LOW cleanup batch(hook + PS env race + AS-29 rename)
- **Parent task:** AS-28(hook .bak)+ AS-29-PS-env-race(ARCH_INDEX 既有)+ AS-29 ID collision rename
- **Budget:** 1.5 h / 80K tokens / 25 steps / 20 min timeout
- **Domain:** none(docs/scripts/hooks)
- **Production LOC est:** ~10(hook 一行 -notmatch→-cnotmatch + .bak 重生 + ARCH_INDEX §7 兩 row)
- **Test LOC est:** 0(無自動化;手測 4 hook scenario)
- **Files likely touched:**
  - `~/.claude/hooks/work-phase-guard.ps1`(OUTSIDE repo;`-notmatch` → `-cnotmatch` + WHY comment + .bak regen)
  - `~/.claude/hooks/work-phase-guard.ps1.bak`(OUTSIDE repo;regenerate)
  - `docs/ARCHITECTURE_INDEX.md` § 7(AS-29 row 描述更精確 + NEW AS-30 row + NEW AS-31 row 若 U-ALS spawn 新 backlog)
  - (可選)`docs/HANDOFF_v0.4.0.1.md` § "does NOT close" 加 1 行 inline note 釐清 AS-29→AS-30 重編
- **Depends on:** none
- **Unlocks:** AS-30(後者必須引用新 ID `AS-30`)
- **Adversarial focus:**
  - Hook `-cnotmatch` 跨 5 scenario test(idle / S-XX uppercase / S-XXa suffix uppercase / s-04 lowercase **應被 block** 變化 vs 之前 unblock / foreign `shop/phase-2` / malformed)
  - .bak header comment 跟 production 同步(沒寫舊版本)
  - ARCH_INDEX § 7 AS-29 描述只留 PS env race;AS-30 row 用 HANDOFF_v0.4.0.1 § "AS-29 fixture" 原文
  - 不動 ArchSim repo 程式碼一行(純 docs + OUTSIDE hook)
  - AS-29 在 ARCH_INDEX 跟 HANDOFF 名稱對齊(若用 NEW row 法,HANDOFF inline 不改也 OK 因為 HANDOFF 是 history)
- **ESCALATE if:**
  - `-cnotmatch` 改變的行為意外擋住 live /work session 流(主執行緒當下也是 hook 影響範圍)
  - HANDOFF 需要重大改寫(over-scope)

### U-ALS — AAlsCharacter::RefreshMeshProperties PIE crash 深掘
- **Parent task:** ALS character mesh null deref(HANDOFF_v0.4.0.1)
- **Budget:** 4 h / 200K tokens / 50 steps / 30 min hard timeout per dispatch(允許二次 dispatch 若 first dispatch 30 min 內回報 root cause 但需要更多時間 implement)
- **Domain:** ue5-engineer(primary;ALS knowledge + UE5.7 reflection)+ cpp-engineer(secondary;PIE crash debug)
- **Production LOC est:** ~30(ArchSimCharacter override / mesh UPROPERTY default / 或 ALS plugin small patch)
- **Test LOC est:** ~50(headless test 若可 + PIE smoke 驗證 ini revert)
- **Files likely touched:**
  - `Source/ArchSim/Public/Characters/ArchSimCharacter.h`(可能加 mesh UPROPERTY default override)
  - `Source/ArchSim/Private/Characters/ArchSimCharacter.cpp`(可能加 BeginPlay null-guard / mesh assignment)
  - `Config/DefaultEngine.ini`(verify `GlobalDefaultGameMode=/Script/ArchSim.ArchSimGameMode` 沒回到 GameModeBase;v0.4.0.1 已 revert)
  - `Source/ArchSim/Public/ArchSimGameMode.h` + `.cpp`(可能 override DefaultPawnClass override 流程)
  - `Plugins/ALS/Source/ALS/Private/AlsCharacter.cpp`(read-only first;只有 ESCALATE 後才動 ⇒ deep dive 升級觸發)
  - (可能)Content/ 下 BP_ArchSimCharacter asset(gitignored;user-side BP mesh 重綁)
- **Depends on:** none
- **Unlocks:** v0.5.0 hard gate 之 P12(PIE input runtime with ArchSimGameMode)
- **Adversarial focus:**
  - Crash root cause **有具體 file:line evidence**(UE log stack trace + AAlsCharacter source line)— 不接受「heuristic guess」
  - Fix 不 regress AS-15(Enhanced Input lifecycle)/ AS-16(CalcCamera override)/ AS-03(ALS character class)
  - Headless 148-UE-test gate 仍 PASS
  - PIE smoke 真的能跑 ArchSimGameMode + ALS char(不是只「不 crash」要能看 third-person view)
  - 若動 ALS plugin source:disclose deviation from upstream + 加 `// FIX(v0.5.0): ...` comment + 上游 issue 連結(若有)
- **ESCALATE if:**
  - 30 min 內無 root cause hypothesis(scope = 4 hour 但給 30 min budget 確認可行性;不收斂 → 主執行緒選 abandon / shallow-only / 接 ini hack ship)
  - Root cause 在 ALS plugin AND fix 非 trivial(> 30 LOC)→ 主執行緒 adjudicate plugin patch vs subclass override
  - 任何 fix attempt 想動 FROZEN paths → 硬停
  - 任何 fix attempt 想動 `.gitignore` / `ArchSim.uproject` / `Plugins/LevelSim/` / build artifact → 硬停
  - 累計超過 4 h cumulative subagent wall-clock 仍無 PASS 修復 → ESCALATE 接 ini-hack-fallback

### AS-30 — Scenario valid-frame fixture + boundary support API
- **Parent task:** AS-29 rename → AS-30(HANDOFF_v0.4.0.1 § "does NOT close")
- **Budget:** 9 h / 250K tokens / 50 steps / 30 min hard timeout per dispatch(可能 2 iteration;最多 3 iteration per Phase 3 BLOCKER cap)
- **Domain:** ue5-engineer(primary;BP-callable + Widget pattern + WITH_EDITOR)+ cpp-engineer(secondary;FrameCoreUE node-snap math + FFrameSupport USTRUCT)
- **Production LOC est:** 250–350(3 新 BP-callable + node-snap helper + portal-frame builder + USTRUCT extension if needed)
- **Test LOC est:** ~150(新 ArchSimScenarioFixtureTest.cpp:6-8 sub-checks + 更新既有 3 個 ScenarioWidget/SolveWire/Tutorial test 若 API surface 改變)
- **Files likely touched:**
  - `Source/ArchSim/Public/Editor/ArchSimScenarioWidget.h`(擴 3 個 method + helper struct/typedef)
  - `Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp`(impl + node-snap loop + portal-frame template)
  - `Source/ArchSim/Private/Tests/ArchSimScenarioFixtureTest.cpp`(NEW)
  - `Scripts/run_gate.ps1`($ExpectedUeTests 148 → 149 cuDSS / 146 → 147 non-cuDSS)
  - `docs/logs/S-05/u3_pie_smoke.md`(P10/P11 改用 SpawnDefaultPortalFrame + expected heatmap colour pattern)
  - (可能)`docs/ARCHITECTURE_INDEX.md` § 2 ArchSimScenarioWidget row(新增 PlaceFixedSupport / SpawnDefaultPortalFrame 描述)— Phase 5 處理
- **Depends on:** U-LOW(ID rename 必須先在 ARCH_INDEX 落地,subagent 才能引用新 ID)
- **Unlocks:** PIE smoke P10/P11 heatmap 綠(v0.5.0 final hard gate)
- **Adversarial focus:**
  - `SpawnDefaultPortalFrame()` 產生的 model **真的能解**(node-snap 共節點 + PlaceFixedSupport 釘對節點 → DoF 約束足夠;不是 12-DoF mechanism)
  - `PlaceFixedSupport` idempotent(重複呼叫同 location 不複製 boundary entry)
  - Node-snap tolerance(預設例 5 cm)合理 + 不合併不相關 member(< 1 member length)
  - WITH_EDITOR guard 在所有新 method/UPROPERTY/test 上(packaged build 0 leak)
  - PIE smoke P10/P11 expected heatmap colour 有解析 oracle(哪根 member 應 red/yellow/green 因 portal-frame 簡單可手算 N/M 比)
  - 5-leg headless gate 仍 PASS(`$ExpectedUeTests` 同步 bump)
  - 不破 v0.4.0.1 的 cross-world fix(K1/K2/K4 用 PIE-world preference 已對;PlaceFixedSupport 用同樣 PIE preference 一致)
  - 不調 FrameCore engine API(FROZEN);若 FFrameSupport USTRUCT 已存在 in FrameCoreUE → reuse;若沒 → 在 FrameCoreUE 加(NOT FROZEN per ARCH_INDEX §3)
- **ESCALATE if:**
  - Fixture API 超過 3 個新 BP-callable method(防 scope creep)
  - Node-snap 需要在 FrameCore/ 加 helper(FROZEN)
  - `SpawnDefaultPortalFrame` 算 solver result 出 NaN/singular(fixture 設計問題,不是 widget bug)
  - $ExpectedUeTests bump 跟既有 test 名字衝突
  - PIE smoke P10/P11 試 3 次仍失敗 → ESCALATE adjudicate fixture 改設計

---

## Risk-adjusted ordering rationale

**Experimental risk profile + 「拼到全部 accept」budget mode** 下:

- **Round 1 並行 3 unit** — U-IWYU 跟 U-LOW 都 ≤2 h 低風險;U-ALS 給 4 h 高風險先 expose root cause。若 U-ALS Round 1 內 ESCALATE → 主執行緒可在 Round 1 結束前(其他 2 unit 還在跑時)決定 abandon / shallow / ini-hack-accept,不浪費 AS-30 大投資。
- **Round 2 序列 AS-30** — 最大 unit(9 h)放最後;前 3 unit 都進 working tree 後再動 widget extension。AS-30 依 U-LOW(AS-30 ID rename 必須在 ARCH_INDEX § 7 才能 subagent 引用)— 故 U-LOW 必須先 accept。
- **不能單 unit 失敗就中斷** — 「拼到全部 accept」=ESCALATE 時 main thread 決定 abandon/replace/continue,不是 stop session。U-ALS 失敗的 fallback = ini-hack 接受 + 把 ALS 真修移到 S-07 backlog,其他 3 unit 仍 ship 進 v0.5.0。
- **PIE smoke 在最後** — USER-DRIVEN 必須由你執行 + 評估;主執行緒不能代行。若 PIE smoke 失敗 → Mode C v0.5.0-rc + S-07 fix 路徑(scope contract Mode 決策樹第 3 條)。

---

## Critical path detail(成功路徑)

```
T = 0        Round 1 dispatched (3 parallel)
              ├── U-IWYU dispatched
              ├── U-LOW dispatched
              └── U-ALS dispatched

T = 1.5 h    U-IWYU returns → Phase 3 review (30 min)
T = 1.5 h    U-LOW returns → Phase 3 review (30 min)
T = 2.0 h    U-IWYU accepted (NITS or CLEAN)
T = 2.0 h    U-LOW accepted (NITS or CLEAN) → ARCH_INDEX § 7 updated
T = 4.0 h    U-ALS returns → Phase 3 review (30 min)
              [若 ESCALATE earlier → 視 fallback 而定]

T = 4.5 h    U-ALS accepted OR ini-hack-fallback adjudicated
T = 4.5 h    Phase 4 batch mid-sprint commits (3 unit 一批;LOW 可能不 commit if OUTSIDE-repo)
T = 4.5 h    Phase 5 mid-sprint docs sync (ARCH_INDEX § 7 ticks)
T = 4.5 h    Round 2 dispatched: AS-30

T = 13.5 h   AS-30 returns → Phase 3 review (30 min)
T = 14.0 h   AS-30 accepted
T = 14.0 h   Phase 4 AS-30 mid-sprint commit
T = 14.0 h   Phase 5 mid-sprint docs sync (ARCH_INDEX § 2 class map + § 6 test inventory 148→149)

T = 14.0 h   USER PIE smoke prompt → 等你執行
T = 14.5 h   USER PIE smoke 完成回報(假設 PASS)

T = 14.5 h   Phase 6 close: release-hardening --minor-bump v0.5.0
T = 16.5 h   v0.5.0 tag 落地(本地);publish commands 印給你
T = 16.5 h   Session 結束
```

**注意:wall-clock 估計含 subagent 並行的 overlap;實際比 sum-of-units(13–22 h)短。若 U-ALS 順利 deep-fix → 此估計準確;若 U-ALS 走 ini-hack-fallback → ALS 路徑停 30–60 min 而非 4 h,critical path 縮短 3 h。**

---

## Session-wide mechanical stops(重申)

| 限制 | 額度 |
|---|---|
| Subagent output tokens(per dispatch) | 300K cap(本 plan 最大 unit 250K) |
| Wall-clock(per dispatch) | 30 min hard timeout(U-ALS / AS-30 可能 2-iteration) |
| Tool calls(per dispatch) | 50 step cap |
| Adversarial re-prompt cycles | 3 BLOCKER cap → AskUserQuestion 升級 |
| Spokes(per dispatch round) | 7 parallel max(本 plan Round 1 = 3 ✓) |
| Session task count | 10 max(本 plan = 4 ✓) |
| AskUserQuestion unavailable | freeze-and-park(work hub § AskUserQuestion-unavailable) |

---

## Adjacent risks / 主執行緒須警覺

1. **U-ALS 真修 vs ini-hack-fallback decision**:若 30 min 內無 root cause → AskUserQuestion 4-option(abandon / shallow-only / ini-hack-accept / extend budget)。**主執行緒不可自行決定 abandon。**
2. **AS-30 fixture 設計 oracle**:portal-frame N/M analytic 値 在動工前先算好(2 column + 1 beam,fixed-fixed base),subagent 才能在 PIE smoke 對照 colour 預期。
3. **PIE smoke u3_pie_smoke.md 加厚版**:S-05 已有 9 section + 16 criteria;AS-30 P10/P11 改成「SpawnDefaultPortalFrame → solve → heatmap 出現 + 3 member colour 對 analytic 預期」;u3_pie_smoke 整體 P 數可能擴到 P15(P12 PIE input + P13 stop + P14 reload smoke + P15 fixture oracle)。
4. **AS-29 ID collision rename 衝突**:HANDOFF_v0.4.0.1 已 publish + tag,內容是 history;不重寫舊 doc,只在 ARCH_INDEX § 7 新增 AS-30 row + 在 HANDOFF inline 加 1 行 note(or skip)。U-LOW subagent 必須清楚這個界線。
5. **FROZEN 守界**:scope contract 已明示;subagent prompt 注入 4-element contract 時必須複述。

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
