# Scope Contract — Session 2026-06-28T0316Z

**Sprint:** S-06
**Target version:** `v0.4.0.1` → **`v0.5.0` (Mode A minor bump,直接跳)**
**Budget estimate:** 13–22h (no wall-clock cap; 拼到 4 unit 全 accept 或使用者主動收尾為止)
**Risk profile:** Experimental — 主動嘗試會炸的東西(ALS plugin deep dive)
**Audience (primary):** 試玩學生 — PIE playable + tutorial 連貫
**Audience (secondary):** 答辯委員 (demo-able); 自己 (學習 ALS / IWYU 機制)
**Language:** zh-TW (繁中)

---

## Goal (success criterion)

把 v0.4 所有實際可行的殘留(HANDOFF_v0.4.0.1 § 「does NOT close」+ ARCH_INDEX §7 可動 backlog + S-05 environmental notes)**一次性收完進 v0.5.0 minor bump**,讓 S-07 從乾淨 baseline 開始。v0.4.x 線到此換代結束。

**Hard gate(v0.5.0 release pre-tag must pass ALL)**:

1. `Scripts\run_gate.ps1 -RequireOpenSees` — 5-leg headless ALL PASS(standalone F1..F71 / UE automation $ExpectedUeTests / OpenSees / linear audit 104 / CLI roundtrip)
2. USER-DRIVEN PIE smoke (參 `docs/logs/S-05/u3_pie_smoke.md` 加厚版) — **P1..P15 全 PASS**,**含 P10/P11 HeatmapActor spawn + colour 綠**
3. FROZEN integrity: `git diff --name-only v0.4.0.1..HEAD -- Plugins/FrameSolver/Source/FrameCore/ Plugins/LevelSim/Source/LevelCore/` 回 0 行

PIE smoke 不再像 v0.4.0 那樣 USER-DRIVEN-post-publish-optional 模式,而是 **release-gate hard requirement** — 必須在 tag 前綠才能 ship。

---

## Tasks (ordered, with budget)

| Order | ID | Title | Budget | Domain skill(s) | Dependency | 風險 |
|---|---|---|---|---|---|---|
| 1 | **U-IWYU** | `Tools/check_iwyu_first_header.py` pre-commit IWYU first-header validator (v0.4.0.1 silent-stale-obj 預防) | 1–2h | cpp-engineer | none | Low |
| 2 | **U-LOW** | LOW cleanup batch: AS-28 hook .bak sync (`-cnotmatch`) + AS-29-PS-env-race 診斷 + AS-29 ID collision rename(原 HANDOFF_v0.4.0.1 的「Scenario fixture」AS-29 改編 AS-30,ARCH_INDEX §7 既有 AS-29 留給 PS-env-race) | 1–2h | none (docs/scripts) | none | Low |
| 3 | **U-ALS** | ALS character mesh null deref 深掘 — `AAlsCharacter::RefreshMeshProperties` PIE crash 根因;先 BP mesh 重綁 / `ArchSimCharacter` UPROPERTY,再評估動 ALS plugin source | 3–6h(可能 6–12h) | ue5-engineer + ALS dive | none(獨立於 widget) | **High** — Experimental,不收斂 → ESCALATE |
| 4 | **AS-30** | Scenario valid-frame fixture + boundary support API: `PlaceFixedSupport(FVector LocationWorld)` + `SpawnDefaultPortalFrame()` + node-snap logic + isolated tests + u3_pie_smoke.md P10/P11 更新 | 8–12h | ue5-engineer + cpp-engineer | U-LOW 完成(因為要 rename) | Med-High |

**Order rationale:**
- U-IWYU + U-LOW **並行** Round 1(無 source collision;預計 ≤2h 內完;U-LOW 必須先過因為 AS-30 用到新 ID)
- U-ALS **Round 2 序列**(獨立於 widget,Experimental risk,先確認可不可走通)
- AS-30 **Round 3 序列**(最大、最後;前 3 unit 都進 working tree 才動 widget extension)

替代分支:若 U-ALS Round 2 30min 內判定 deep dive 不可行 → 直接 ESCALATE,使用者選「接受 ini hack 繼續 ship」或「abandon U-ALS,只 ship 其餘 3 unit 為 v0.5.0」。

---

## Constraints

- **FROZEN 全守(鐵則 #1)**:`Plugins/FrameSolver/Source/FrameCore/` + `Plugins/LevelSim/Source/LevelCore/` 必須 `git diff --name-only v0.4.0.1..HEAD` 回 0 行
- **Never-touch(鐵則 #5)**:`.gitignore` / `ArchSim.uproject` / `Plugins/LevelSim/` 整目錄 / build artifacts(`*.dll/.exp/.lib/.obj/.pdb/.exe`)
- **commit hygiene(鐵則 #5)**:顯式 `git add <file>`,絕不 `-A` 或 `.`
- **ArchSimScenarioWidget.h serial-only**:S-05 lesson — 若多個 dispatch unit 同時動該檔則必須序列化(本 contract 只有 AS-30 動,序列化自動成立)
- **Honest verify(鐵則 #3)**:`[NEW CODE]` vs `[VERIFIED]` 誠實分級;主執行緒不可代 subagent 改 `[VERIFIED]` label
- **5-leg gate 在 commit 前必跑**(鐵則 #2):每次 mid-sprint feature commit 之前 main thread 跑一次完整 gate,記錄結果

---

## Anti-goals

1. **不動 FROZEN**(明規 — 鐵則 #1 預設):若任何 subagent 想動 FrameCore/ 或 LevelCore/ 即 ESCALATE
2. **不動 FrameCoreUE plugin source**(本 v0.5.0 全集中在 ArchSim game-body + BP + Editor widget;FrameCoreUE 屬於 S-07 之後 next-gen feature scope)
3. **不重排 v0.4.x history**(v0.4.0 prerelease 標記保留;v0.4.0.1 stable 不動;v0.5.0 從 v0.4.0.1 之上開新 commit)
4. **不開 GitHub release / push remote 不經使用者同意**(release-hardening 既有規則)
5. **不假裝 PIE smoke pass**:若 USER PIE smoke P10/P11 沒綠,絕不 mark v0.5.0 release-ready;走 v0.5.0-rc / pivot 路徑

---

## ESCALATE conditions(subagent 必須停下來問)

主執行緒從 S-05 lessons + 本 scope 風險畫面套用 7 條:

1. **任何想動 FROZEN paths 的需求** → ESCALATE,主執行緒收 → 走 CLAUDE.md amendment 流程(本 session 預設拒)
2. **U-ALS deep dive 6h cumulative 無 root cause** → ESCALATE,選「接受 ini hack 繼續 ship」/「abandon U-ALS, ship 3-unit v0.5.0」
3. **AS-30 fixture API surface 超過 3 個新 BP-callable method**(原 plan: PlaceFixedSupport + SpawnDefaultPortalFrame + 1 helper) → ESCALATE 防 scope creep
4. **subagent 想新增 dispatch unit / 改 scope contract** → ESCALATE(只有主執行緒能改 contract)
5. **5-leg gate 任何 leg FAIL 且非環境性 race** → ESCALATE(不可 silent-fix 後 claim PASS)
6. **PIE smoke 試跑 4 次仍 P10/P11 FAIL** → ESCALATE(可能是 fixture 設計問題,不是 widget bug)
7. **iteration > 3 BLOCKER 循環**(Phase 3 review 標準)→ AskUserQuestion 4-option 升級

無 wall-clock cap → 不在 ESCALATE 觸發列(由使用者主動或自然收斂收尾)

---

## Mode 決策樹(Phase 6 close 時)

- **4 unit 全 accept + 5-leg + PIE smoke 全綠** → `release-hardening --minor-bump v0.5.0` ship as planned
- **3 unit accept,U-ALS abandon(ini hack 保留)** → ship `v0.5.0` 但 release notes 誠實列「ALS deep deferred to S-07,ArchSimGameMode 配 ini fallback 暫存」+ AS-31 backlog
- **U-ALS + AS-30 都 accept 但 PIE smoke P10/P11 失敗** → ship `v0.5.0-rc` annotated tag(非正式 release),next session 接 P10/P11 fix
- **使用者中途收尾(park signal)** → Phase 6 Mode C parked,把未完成 unit 寫進 `HANDOFF_v0.5.0-partial.md` recommended scope

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
