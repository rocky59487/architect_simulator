# Execution Plan — Sprint S-05 (drafted 2026-06-27T02:00 Asia/Taipei)

**Scope contract:** [`docs/logs/S-05/scope_2026-06-27T0145.md`](scope_2026-06-27T0145.md)
**Total dispatch units:** 8 (3 Path B patches + 1 UE5.8 sandbox eval + 3 Scenario MVP spikes + 1 conditional release)
**Critical path estimate:** ~12-15h success path / ~3-4h fall-back path
**Critical-path bottleneck:** SPIKE-Scenario-u3 (K2+K4 + tutorial+voice/prompt + PIE 5min smoke); fall-back triggers RELEASE-v0.3.2
**Parallelism opportunities:** Round 1 dispatches 4 units in parallel (3 patches + UE5.8 eval — no file collision); Round 2-4 sequential (u1→u2→u3 chain)

---

## Dispatch graph

```
Round 1 (parallel, ~45-60 min):
  ┌─────────────────────────┐
  │ AS-26-u1   AS-27-u1   AS-25-u1   SPIKE-UE5.8-eval │
  │   (ArchSim)  (ArchSim)  (OUTSIDE)  (Research/ sandbox) │
  └──────────────┬──────────┘
                 │ all 3 patches accepted → pivot trigger fires (no tag)
                 ▼
Round 2 (sequential, ~3-4h):
              SPIKE-Scenario-u1  (skeleton + K1 placement)
                 │
                 ▼
Round 3 (sequential, ~3-4h):
              SPIKE-Scenario-u2  (wire Registry→Solve→Heatmap)
                 │
                 ▼
Round 4 (sequential, ~3-5h):
              SPIKE-Scenario-u3  (K2+K4 + tutorial + voice/prompt + PIE 5min smoke)
                 │
        ┌────────┴────────┐
   PASS │                 │ FAIL
        ▼                 ▼
Round 5a (~1.5-2h):    Round 5b (~1h):
RELEASE-v0.4.0       RELEASE-v0.3.2 fall-back
(Scenario MVP        (AS-25/26/27 bundle only;
 + 3 patches)         Scenario WIP NOT in tag)
```

**Independent track:** `SPIKE-UE5.8-eval` runs in Round 1 alongside the 3 patches because it lives entirely under `Research/ue58_attempt/` + writes decision doc to `docs/logs/S-05/ue58_eval.md`. No main-worktree contention.

---

## Unit details

### AS-26-u1: `UArchSimModelRegistry` ClassWithin verify + `ArchSimPieHarness` `NewObject` outer mirror

- **Budget:** 30-45 min / 100K tokens / 25 steps / 20min timeout
- **Domain:** ue5-engineer (primary) + cpp-engineer (secondary)
- **Files:**
  - `Source/ArchSim/Public/Subsystems/ArchSimModelRegistry.h` (READ-only — verify implicit ClassWithin via base `UGameInstanceSubsystem`)
  - `Source/ArchSim/Private/Tests/ArchSimPieHarness.cpp:81` (PRODUCTION — change `NewObject<UArchSimModelRegistry>()` → `NewObject<UArchSimModelRegistry>(GetTransientPackage())` + AS-24-style WHY comment)
  - `Scripts/run_gate.ps1` (READ — confirm `$ExpectedUeTests` unchanged at 145/143)
- **Production code delta estimate:** +3 LOC code + ~5 LOC comment (1 line + WHY block)
- **Test delta estimate:** 0 (mirror should not change any test behaviour)
- **Pre-flight grep:** verified — `ArchSimPieHarness.cpp:81` confirmed `return NewObject<UArchSimModelRegistry>();` at line 81; `ArchSimModelRegistry.h:33-34` UCLASS has no explicit `Within=` (inherits from `UGameInstanceSubsystem`)
- **Adversarial focus:**
  - ClassWithin verification: confirm `UGameInstanceSubsystem` source has implicit `Within=UGameInstance` (cite UE engine source path:line)
  - WHY comment style matches AS-24 pattern at `FrameCoreUEInteractiveSubsystemTest.cpp` (compare side-by-side)
  - 5-leg gate count unchanged (145 cuDSS / 143 non-cuDSS)
  - No collateral edit (only line 81 + comment block above)
- **Adversarial skip:** generic UE5 API style; ALS/SPUD/SUQS unrelated touch
- **Evidence required:** UE5.7 engine path:line for ClassWithin inheritance; 5-leg gate verbatim PASS readout
- **ESCALATE triggers:**
  - ClassWithin verification shows `UArchSimModelRegistry` does NOT actually have Within=UGameInstance — the AS-26 hypothesis is wrong; bring evidence + propose 2 alternative fixes
  - Test behaviour changes after the fix (sub-check failure / count drift)
- **Depends on:** none
- **Unlocks:** SPIKE-Scenario-u1 (cleared file ownership in test fixture area)

### AS-27-u1: ARCH_INDEX §8 stale numbers + `ArchSimPieDriverLoopTest` empirical comments (2-item cosmetic bundle)

- **Budget:** 10-20 min / 60K tokens / 15 steps / 15min timeout
- **Domain:** cpp-engineer (primary)
- **Files:**
  - `docs/ARCHITECTURE_INDEX.md` § 8 gate cheat-sheet around L289 (DOCS — `140 expected / 138 on non-cuDSS` → `145 expected / 143 on non-cuDSS`)
  - `Source/ArchSim/Private/Tests/ArchSimPieDriverLoopTest.cpp:54+L58` (TEST comment — "always has at least one" / "commandlet always provides" → empirical phrasing matching NIT-a closeout in PieHarness.h:52)
- **Production code delta estimate:** 0 (test comments + docs only)
- **Test delta estimate:** ~2 lines comment text (no logic)
- **Pre-flight grep:** verified — DriverLoopTest L54+L58 confirmed contain "always has at least one" + "commandlet always provides" empirical-overclaim text per HANDOFF §4
- **Adversarial focus:**
  - ARCH_INDEX §8 exact diff: 140→145, 138→143, no collateral edit
  - DriverLoopTest comments: only phrasing change, no logic change (run isolated test, confirm 7 sub-checks still PASS)
  - 5-leg gate count unchanged (145/143)
- **Adversarial skip:** §7 backlog table edits (already done in S-04 PHASE5-NITS-u1)
- **Evidence required:** before/after diff of the 2 files; 5-leg gate PASS verbatim
- **ESCALATE triggers:**
  - ARCH_INDEX §8 has additional stale numbers not in scope (escalate to bundle vs defer)
  - DriverLoopTest behaviour changes after comment edit (impossible barring tab/encoding issues)
- **Depends on:** none
- **Unlocks:** RELEASE-v0.3.2 or RELEASE-v0.4.0 (cleanup bundle component)

### AS-25-u1: Hook regex broaden `^S-\d+$` → `^S-[\w]+$` (OUTSIDE repo, ceremonial accept)

- **Budget:** 10-15 min / 50K tokens / 15 steps / 10min timeout
- **Domain:** (no domain skill — outside-repo PowerShell hook)
- **Files:**
  - `~/.claude/hooks/work-phase-guard.ps1` (PRODUCTION — content-sniff regex broaden)
- **Production code delta estimate:** ~3 LOC (regex + comment)
- **Test delta estimate:** 0 (re-run U-INFRA-u1's 4-scenario stdin test)
- **Pre-flight grep:** N/A (outside repo; subagent reads hook before editing)
- **Adversarial focus:**
  - Regex correctness: `^S-[\w]+$` matches `S-04`, `S-04a`, `S-04b`, `S-XXa`, rejects garbage like `foo` / `S-04!`
  - U-INFRA-u1 4-scenario stdin test re-run PASS
  - .bak backup taken before edit; hook restored if fail
  - No ArchSim repo commit (ceremonial accept = agent log only; this is OUTSIDE rep)
- **Adversarial skip:** any ArchSim-repo gate check
- **Evidence required:** 4-scenario test verbatim; hook file diff
- **ESCALATE triggers:**
  - Hook regex change breaks 4-scenario test in unexpected way
  - .bak restoration fails for any reason
- **Depends on:** none
- **Unlocks:** none in this sprint (carry-forward benefit only)

### SPIKE-UE5.8-eval: UE5.8 install + plugin compatibility evaluation (parallel sandbox)

- **Budget:** 2-3h / 200K tokens / 35 steps / 25min timeout (per-dispatch; total may need 2 dispatches)
- **Domain:** ue5-engineer (primary)
- **Files (NEW — sandbox):**
  - `Research/ue58_attempt/` (NEW directory; sandbox for any UE5.8-version uproject + plugin tests)
  - `Research/ue58_attempt/README.md` (NEW; eval methodology)
  - `docs/logs/S-05/ue58_eval.md` (NEW; decision doc with GO/NO-GO/CONDITIONAL for S-06 upgrade)
- **Production code delta estimate:** 0 (no main worktree change)
- **Test delta estimate:** 0
- **Pre-flight grep:** confirmed `Research/ue58_attempt/` does NOT exist (fresh sandbox)
- **Adversarial focus:**
  - Sandbox isolation: 0 lines change in `ArchSim.uproject`, `Plugins/*`, `Config/`, `Source/*`
  - UE5.8 install detection: honest report of install presence + version + path
  - 4-plugin compat status (ALS / SPUD / Prefabricator / SUQS): per-plugin yes/no/unknown with reason
  - SPUD `StructUtils` deprecation: confirm warning becomes error in UE5.8 (or graceful fallback?)
  - Decision doc explicit: GO (S-06 can upgrade) / NO-GO (block until fixed) / CONDITIONAL (with caveat list)
- **Adversarial skip:** main worktree integrity checks unrelated to UE5.8 compat
- **Evidence required:** UE5.8 install path screenshot/text output; per-plugin build try-attempts; conclusion doc
- **ESCALATE triggers:**
  - UE5.8 install NOT available on host → return early with NO-GO decision doc + "first action S-06: install UE5.8" (counts as honest fail, not escalation)
  - Sandbox attempt would require touching main `ArchSim.uproject` (rule #5 violation) → ESCALATE
  - 3+ plugins fail UE5.8 build with non-trivial errors (decision doc still useful even if abandoned)
- **Depends on:** none (independent of main worktree)
- **Unlocks:** S-06 UE5.8 upgrade decision input (no impact on RELEASE-v0.4.0 this sprint)

### SPIKE-Scenario-u1: Editor Utility Widget skeleton + K1 placement → Registry register

- **Budget:** 3-4h / 250K tokens / 50 steps / 30min timeout
- **Domain:** ue5-engineer (primary) + game-designer (secondary; for K1 = column placeholder UX choices)
- **Files (NEW):**
  - `Source/ArchSim/Public/Editor/ArchSimScenarioWidget.h` (NEW; `#if WITH_EDITOR`-guarded; class `UArchSimScenarioWidget : UEditorUtilityWidget`)
  - `Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp` (NEW; `#if WITH_EDITOR` impl)
  - `Source/ArchSim/ArchSim.Build.cs` (PRODUCTION — append `"EditorScriptingUtilities", "Blutility", "UMG", "UMGEditor"` to PrivateDependencyModuleNames inside `if (Target.Type == TargetType.Editor)` block)
  - `Source/ArchSim/Private/Tests/ArchSimScenarioWidgetTest.cpp` (NEW; headless smoke for widget class reflection + 1 K1 placement → RegisterMember signature)
  - `Scripts/run_gate.ps1` (PRODUCTION — `$ExpectedUeTests` 145 → 146 / 143 → 144)
- **Files (READ-only — precedent):**
  - `Plugins/FrameSolver/Source/FrameCoreUE/Public/SFrameCoreStressFieldPanel.h` (Slate panel precedent from v3.2.0)
  - `Source/ArchSim/Public/Components/ArchSimMemberData.h` (RegisterMember BP-callable surface)
- **Production code delta estimate:** ~100-150 LOC (widget class + 1 K1 placement spawner) + Build.cs ~5 LOC append
- **Test delta estimate:** ~80 LOC headless smoke (5-7 sub-checks; CDO reflection + K1 spawner signature + Registry register pass-through)
- **Adversarial focus:**
  - `WITH_EDITOR` guard correctness: 0 runtime code path on shipping config (test build script if available)
  - Build.cs Editor-only module deps correctly gated by `if (Target.Type == TargetType.Editor)`
  - K1 placement → `UArchSimMemberData` BP-callable register convention matches AS-A1-* precedent
  - Widget class reflection: `GetClass()->IsChildOf(UEditorUtilityWidget::StaticClass())` PASS
  - No FROZEN engine touch (`Plugins/FrameSolver/Source/FrameCore/`, `Plugins/LevelSim/Source/LevelCore/`)
  - 5-leg gate green at 146/144 (count bumped by 1 new test)
- **Adversarial skip:** voice TTS hooks (deferred to u3); K2/K4 placement (deferred to u3); tutorial overlay (deferred to u3)
- **Evidence required:** Build.cs diff verbatim; new file paths + LOC; gate PASS verbatim with 146/144 readout
- **ESCALATE triggers:**
  - `Source/ArchSim/Editor/` subdir scheme conflicts with existing UE conventions discovered during impl → propose new `ArchSimEditor` module instead (request user adjudication)
  - K1 placeholder placement requires `AArchSimCharacter` cooperation that needs production change → ESCALATE
  - `UEditorUtilityWidget` base class not available in UE5.7 Blutility plugin (impossible per UE5.7 docs but ESCALATE if found)
  - Build.cs change requires touching `.uproject` (rule #5 violation)
- **Depends on:** AS-26-u1 (clears `ArchSimPieHarness.cpp:81` test fixture area; no actual file overlap but conceptual ordering)
- **Unlocks:** SPIKE-Scenario-u2

### SPIKE-Scenario-u2: Wire u1 widget to Registry → Solve → Heatmap visualization

- **Budget:** 3-4h / 250K tokens / 50 steps / 30min timeout
- **Domain:** ue5-engineer (primary)
- **Files:**
  - `Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp` (PRODUCTION — extend; add SolveAndDisplay path)
  - `Source/ArchSim/Public/Editor/ArchSimScenarioWidget.h` (PRODUCTION — add UFUNCTION RequestSolve+OnHeatmapReady event)
  - `Source/ArchSim/Private/Tests/ArchSimScenarioWidgetTest.cpp` (TEST — add 3-4 sub-checks for solve dispatch + heatmap actor spawn)
  - `Scripts/run_gate.ps1` (PRODUCTION — `$ExpectedUeTests` 146 → 146 (no new test class; sub-checks add inside existing class); only bump if new IMPLEMENT_SIMPLE_AUTOMATION_TEST)
- **Files (READ-only — precedent):**
  - `Source/ArchSim/Public/ArchSimGameInstance.h` (Tick driver loop convention)
  - `Source/ArchSim/Public/Subsystems/ArchSimModelRegistry.h` (RequestSolve API)
  - `Plugins/FrameSolver/Source/FrameCoreUE/Public/Visual/FrameUtilizationHeatmapActor.h` (heatmap actor surface)
- **Production code delta estimate:** ~150-200 LOC
- **Test delta estimate:** ~80 LOC additional sub-checks
- **Adversarial focus:**
  - RequestSolve dispatch through `UArchSimGameInstance::Tick` 150ms debounce — no double-solve
  - `FFrameSolveResult` round-trip from `UFrameInteractiveSubsystem.SolveLinear` correctly reaches widget
  - `AFrameUtilizationHeatmapActor` spawn/destroy lifecycle handled by widget (no leaked actors on widget close)
  - In Editor Utility widget context (not PIE), heatmap actor must spawn into the Editor world correctly
  - No `static` global mutable state (race in Editor widget hot-reload)
  - 5-leg gate green
- **Adversarial skip:** K2/K4 placement; tutorial UI; voice
- **Evidence required:** Tick → RequestSolve → Solve → Heatmap chain trace via UE_LOG; gate PASS verbatim
- **ESCALATE triggers:**
  - Heatmap actor cannot spawn in Editor world (only PIE) → propose 2 alternatives (procedural mesh debug render vs deferring to PIE-only)
  - Tick-debounce coexistence breaks an existing test (regression) → root-cause and fix or ESCALATE
  - `UFrameInteractiveSubsystem` SolveLinear API requires production change in FROZEN-side (impossible — verify) or in ArchSim consumer side (acceptable change)
- **Depends on:** SPIKE-Scenario-u1
- **Unlocks:** SPIKE-Scenario-u3

### SPIKE-Scenario-u3: K2+K4 + tutorial overlay + voice/prompt UI + reload smoke + manual PIE 5-min student trial

- **Budget:** 4-5h / 300K tokens / 60 steps / 30min timeout (per-dispatch; may need 2 dispatches if voice TTS surfaces ESCALATE)
- **Domain:** ue5-engineer (primary) + game-designer (secondary)
- **Files:**
  - `Source/ArchSim/Private/Editor/ArchSimScenarioWidget.cpp` (PRODUCTION — extend; add K2+K4 spawners + tutorial state machine + voice TTS hook (text-only if no SDK))
  - `Source/ArchSim/Public/Editor/ArchSimScenarioWidget.h` (PRODUCTION — add tutorial state enum + voice toggle UPROPERTY)
  - `Source/ArchSim/Private/Tests/ArchSimScenarioWidgetTest.cpp` (TEST — add 3-5 sub-checks for K2+K4 spawn + tutorial state machine + reload smoke)
  - `docs/logs/S-05/u3_pie_smoke.md` (NEW — PIE 5min student trial smoke report; screenshot/video reference)
  - `Scripts/run_gate.ps1` (PRODUCTION — `$ExpectedUeTests` 146 → 146 (or 147 if K2+K4 spawn test class new))
- **Files (READ-only):**
  - `Source/ArchSim/Public/Components/ArchSimMemberData.h` (K-set placement contract)
  - `Plugins/FrameSolver/Source/FrameCoreUE/Public/Visual/FrameUtilizationHeatmapActor.h`
- **Production code delta estimate:** ~250-350 LOC (K2+K4 spawners + tutorial overlay state machine + voice TTS hook)
- **Test delta estimate:** ~120 LOC additional sub-checks
- **Adversarial focus:**
  - K2 (typically beam) + K4 (typically diagonal brace) placement → proper geometry → solve success (cantilever-like sanity oracle)
  - Tutorial overlay state machine: initial → K1-prompt → K2-prompt → K4-prompt → solve-prompt → free-explore (all states reachable; no deadlock)
  - Voice TTS: if Windows built-in `SAPI` available (no 3rd-party SDK), use it; otherwise fall back to text-only (don't ESCALATE for SDK absence — text-only is acceptable degradation)
  - Reload smoke: open widget → place K1 → close widget → reopen → state cleared (no stale actors)
  - **Manual PIE 5min student trial smoke**: subagent CANNOT run real PIE in headless mode → subagent reports headless smoke + the PIE 5min smoke is a HUMAN-DRIVEN gate. Subagent's job = ensure headless smoke PASS + write the PIE smoke instructions to `u3_pie_smoke.md` for the integrator (you) to perform.
  - **PIE 5min gate predicate** (you, after subagent return):
    1. Launch UE Editor → Open Editor Utility widget → start tutorial
    2. Step through K1 → K2 → K4 prompts (all 3 placement)
    3. Trigger solve → heatmap shows
    4. Transition to free-explore mode → place 5 more random K-set items → 5 successive solves
    5. Close widget cleanly (no actor leak, no crash, no deadlock)
    6. Duration ≥ 5 minutes of unattended PIE session
  - No FROZEN engine touch
  - 5-leg gate green
- **Adversarial skip:** none — this is the finale
- **Evidence required:** headless smoke PASS; PIE smoke instructions doc; voice TTS choice rationale (SAPI vs text-only)
- **ESCALATE triggers:**
  - Voice TTS requires 3rd-party SDK (e.g. `pip install pyttsx3`, Azure TTS, ElevenLabs) — ESCALATE for user adjudication of dep choice + license
  - Tutorial state machine requires new BP base class that conflicts with `UEditorUtilityWidget` → propose alternative architecture
  - Reload smoke shows leaked heatmap actor — root cause and fix or ESCALATE
  - PIE 5min smoke can be deferred / partially-deferred only with user authorization
- **Depends on:** SPIKE-Scenario-u2
- **Unlocks:** RELEASE-v0.4.0 (conditional on PIE 5min PASS) / RELEASE-v0.3.2 (fall-back)

### RELEASE-v0.4.0 (conditional): Minor bump for Scenario MVP playable

- **Budget:** 1.5-2h / 200K tokens / 50 steps / 30min timeout
- **Domain:** release-hardening skill (delegated by Phase 4)
- **Files (NEW):**
  - `docs/RELEASE_v0.4.0.md` (NEW; 10-section template per v0.3.x precedent)
  - `docs/HANDOFF_v0.4.0.md` (NEW; 6-section template)
- **Files (PRODUCTION update):**
  - `docs/ARCHITECTURE_INDEX.md` (Latest tag + § 2 class map for `UArchSimScenarioWidget` + § 6 UE test count bump + § 7 backlog table)
  - `docs/logs/S-05/manager.md` (full sprint log)
- **Pre-condition (HARD GATE):** u3 PIE 5min smoke PASS as adjudicated by user
- **Adversarial focus (release-hardening Phase 1-7 standard):**
  - All 5 units' shipped commits properly attributed
  - 5-leg gate PASS at v0.4.0 tag commit
  - FROZEN integrity: `git diff --stat v0.3.1..HEAD -- Plugins/FrameSolver/Source/FrameCore/ Plugins/LevelSim/Source/LevelCore/` returns 0 lines
  - No hardcoded paths / privacy leakage in committed files
  - Cross-doc numeric consistency
- **Evidence required:** all release-hardening Phase 1-7 standard outputs
- **ESCALATE triggers:**
  - PIE 5min smoke gate not actually PASSed (user did not perform it) — defer to RELEASE-v0.3.2
  - FROZEN integrity violation — block tag
- **Depends on:** SPIKE-Scenario-u3 (PIE 5min smoke PASS)
- **Unlocks:** Phase 5 docs sync + Phase 6 close

### RELEASE-v0.3.2 (fall-back conditional): Patch bundle (AS-25/26/27 only)

- **Budget:** 1h / 150K tokens / 35 steps / 25min timeout
- **Domain:** release-hardening skill (delegated by Phase 4)
- **Files (NEW):**
  - `docs/RELEASE_v0.3.2.md` (NEW)
  - `docs/HANDOFF_v0.3.2.md` (NEW)
- **Files (PRODUCTION update):**
  - `docs/ARCHITECTURE_INDEX.md` (Latest tag + § 7 backlog table)
  - `docs/logs/S-05/manager.md` (full sprint log with Path A WIP commit attribution)
- **Pre-condition:** u3 PIE 5min smoke FAIL OR user signals close before u3 completes
- **Specifics:** Path A WIP commits (u1/u2/u3 partial) remain in working tree but NOT in v0.3.2 tag. Tag commit specifically bundles AS-25 + AS-26 + AS-27 cleanup + release-hardening only.
- **Adversarial focus (release-hardening lighter):**
  - Path A WIP commits explicitly excluded from v0.3.2 tag scope
  - 5-leg gate PASS at v0.3.2 tag commit (which may need temp stash of Path A WIP or explicit cherry-pick)
  - FROZEN integrity unchanged
- **Evidence required:** stash/cherry-pick rationale doc; gate PASS verbatim
- **ESCALATE triggers:**
  - Path A WIP commits cannot be cleanly excluded without losing Scenario progress — propose 3 alternatives (temp branch / stash / partial restore)
- **Depends on:** SPIKE-Scenario-u3 outcome OR explicit user "park here" signal
- **Unlocks:** Phase 5 docs sync + Phase 6 close

---

## Parallel collision check

| Round | Units | Files claimed | Collision? |
|---|---|---|---|
| 1 | AS-26-u1 | `Source/ArchSim/Private/Tests/ArchSimPieHarness.cpp` | (claim) |
| 1 | AS-27-u1 | `docs/ARCHITECTURE_INDEX.md` + `Source/ArchSim/Private/Tests/ArchSimPieDriverLoopTest.cpp` | (claim) |
| 1 | AS-25-u1 | `~/.claude/hooks/work-phase-guard.ps1` (OUTSIDE repo) | (claim) |
| 1 | SPIKE-UE5.8-eval | `Research/ue58_attempt/*` (NEW) + `docs/logs/S-05/ue58_eval.md` (NEW) | (claim) |

**Collision check verdict:** **NO COLLISION**. AS-26 + AS-27 both touch `Source/ArchSim/Private/Tests/` but distinct files (PieHarness.cpp vs PieDriverLoopTest.cpp). AS-25 is OUTSIDE repo. UE5.8 sandbox is `Research/` + new doc. All 4 Round 1 units parallel-safe.

**Round 1 carry-over note:** All 4 units will run 5-leg gate on combined working tree. Per S-04 lesson #7, this is acceptable for gate-PASS reading because the diffs do not interact semantically. Each unit's Phase 4 commit uses **explicit per-file `git add`** so commits stay attributable.

---

## Risk-adjusted ordering rationale

Per scope contract Risk = Experimental, ordering rule = "risky units FIRST". However:

1. **Path B (AS-25/26/27) is Safe-tier** — bundling these in Round 1 builds confidence + clears the patch-line backlog before the Experimental Scenario sub-band starts.
2. **SPIKE-UE5.8-eval is Medium-tier independent** — Round 1 parallel because it's a separate worktree concern; eval result feeds S-06 planning, not S-05 execution. If it ESCALATEs early (no UE5.8 install), no impact on Scenario critical path.
3. **SPIKE-Scenario-u1/u2/u3 is Experimental-tier escalating** — sequential because dependency chain; risk monotonically increasing (u1 = scaffold easy, u2 = wire moderate, u3 = K2+K4+tutorial+voice+PIE smoke high).

**Critical path bottleneck:** SPIKE-Scenario-u3 PIE 5min smoke gate. If it FAILs, we ship v0.3.2 fall-back (Path B alone). If u3 ESCALATEs voice TTS dep before PIE smoke, user adjudicates → may degrade to text-only and continue.

**Parallelism opportunities:**
- Round 1: 4-way parallel (collision-safe verified)
- Round 2-4: Sequential (dependency chain on widget code)

---

## Session-wide mechanical stops (re-asserted)

```
Per-dispatch caps (hub-enforced):
- Subagent output tokens: 300K cap (per dispatch)
- Wall-clock per dispatch: 30 min hard timeout
- Tool calls per dispatch: 50 step cap (60 for u3 per S-04 lesson #6)
- Adversarial re-prompt cycles: 3 BLOCKER cap → escalate to user

Session-wide:
- Spokes per dispatch round: 7 parallel max (Round 1 has 4; well under cap)
- Session task count: 10 max (this plan has 8 dispatch units + 1 conditional release = 9; under cap)
- Wall-clock cap: NONE per scope contract (PIE 5min smoke is the only hard gate)
- Compaction recovery: state file + manager.md authoritative source of truth
```

Per S-04 lesson #6 ("step-cap mechanical violation is usually a planning issue"): u3 budget extended to 60 steps because K2+K4+tutorial+voice+PIE smoke instructions doc is multi-piece work.

---

## Critical path detail (success scenario)

```
T=0       Round 1 dispatched (parallel, 4-way):
            AS-26-u1 (45min) | AS-27-u1 (15min) | AS-25-u1 (10min) | SPIKE-UE5.8-eval (2-3h)
T=45min   AS-26 + AS-27 + AS-25 returned; reviews queued.
            UE5.8 eval still running.
T=55min   All 3 patch reviews accepted (NITS-or-CLEAN expected).
T=55min   Phase 4 commits Round 1 (per-unit explicit git add x3); no tag.
T=1h      Pivot trigger fires (3 patches accepted); SPIKE-Scenario-u1 dispatched.
T=4h      SPIKE-Scenario-u1 returns; review; accept (NITS expected).
            UE5.8 eval returned (~2h); decision doc + commit.
T=4.5h    Phase 4 commit SPIKE-Scenario-u1 (no tag); SPIKE-Scenario-u2 dispatched.
T=8h      SPIKE-Scenario-u2 returns; review; accept.
T=8.5h    Phase 4 commit u2 (no tag); SPIKE-Scenario-u3 dispatched.
T=13h     SPIKE-Scenario-u3 returns (longer dispatch + retry if voice ESCALATEs):
            Subagent reports headless smoke PASS + PIE smoke doc written.
            User runs manual PIE 5min smoke gate.
T=13.5h   If PIE smoke PASS:
            Phase 4 commit u3; RELEASE-v0.4.0 dispatched (release-hardening).
T=15h     v0.4.0 tagged locally; publish commands print; Phase 5 docs sync.
T=15h     Phase 6 close; session retrospective; state file → idle.
```

## Critical path detail (fall-back scenario)

```
T=0       Round 1 as above
T=1h      Pivot → SPIKE-Scenario-u1 (in progress)
...       u1/u2 partial WIP
T=??      User signals "park" OR PIE smoke FAIL
T=??+1h   RELEASE-v0.3.2 dispatched (release-hardening fall-back)
T=??+2h   v0.3.2 tagged locally; Scenario WIP commits remain in working tree
T=??+2h   Phase 6 close; manager.md retrospective + S-06 recommended scope
            (top priority: complete Scenario u1/u2/u3 → v0.4.0 in S-06)
```

---

## What this plan does NOT cover (out of scope, will not silently expand)

- **Position-sync feature** for `UArchSimMemberData` (carry-over from AS-02; only registered-count delta is wired) — defers to S-06+ separate AS-XX
- **AS-04/05/06/08/09** carry-overs (per scope contract § 8)
- **`Source/ArchSimEditor/` as separate module** — S-05 uses `WITH_EDITOR` guard in `Source/ArchSim/`; if scope expands need explicit user authorization (Phase 0 re-entry)
- **3rd-party voice TTS SDK** — text-only is acceptable degradation per Plan u3 finding; SDK adoption is S-06+ separate eval

---

*This plan is the authoritative execution graph for Sprint S-05. Phase 2 dispatch consumes this directly per unit; phases 3-6 reference back for verdicts + ceremony.*
